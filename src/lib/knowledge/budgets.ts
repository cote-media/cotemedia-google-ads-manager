// LORAMER_KNOWLEDGE_INGEST_V1 — single source of truth for the Knowledge-store per-scope word budgets.
// Imported by the ingest route (/api/knowledge) and the prompt assembler (build-claude-context).
export const CLIENT_WORD_BUDGET = 25000
export const AGENCY_WORD_BUDGET = 8000
