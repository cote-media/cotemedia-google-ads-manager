# LORAMER_QUEUE_OF_RECORD.md — CONSOLIDATED BACKLOG  ⛔ MANDATORY SESSION READING (read in full, every session)
Consolidated backlog of everything PLANNED-BUT-NOT-BUILT, merged from every queue home into ONE list.
Built 2026-06-22 by reading: ROADMAP.md, AUDIT_FINDINGS.md, LAUNCH_PARKING.md, CONTINUE_HERE.md (all embedded
queues), docs/LORAMER_DATA_COMPLETENESS.md, and the design docs' open backlog.
Rules used: one row per distinct item; duplicates merged (all sources listed); existing LORAMER_*_V1 markers
reused; status = open/partial/blocked/date-gated/status-unclear; launch-relevance per the GOSPEL default
(everything launch-critical unless a named next-phase or external constraint). Grouped by the existing Project
structure. SHIPPED/[x] items live in the DONE appendix (do-not-rebuild). Nothing invented; ambiguous status
marked "status-unclear (verify)". Conflicts resolved from repo ground truth 2026-06-22 (commits cited inline).
RULE: before proposing any action, restate the queued items here that bear on it — to prove you read them.

LEGEND: [LC]=launch-critical · [NP]=next-phase (post-launch) · [EXT]=external-constraint-gated · [DG]=date-gated

═══════════════════════════════════════════════════════════════════
ACTIVE WORKSTREAM — DATA COMPLETENESS PROGRAM (docs/LORAMER_DATA_COMPLETENESS.md + CONTINUE_HERE NEXT STEP)
Governing law: capture EVERYTHING from EVERYWHERE, store FOREVER, full grain + history. [LC]
═══════════════════════════════════════════════════════════════════
- Google 2025-floor clients — confirm forward-only vs new: Inside google(2025-06), Thought Streams google(2025-04), Ennis google(2025-02) — probe pre-2025. src: fleet audit. open(verify) [LC]
- Ennis Exterminating — shallow GA (5 days): run gaBackfillAdapter to floor. src: DATA_COMPLETENESS, CONTINUE_HERE. open [LC]
- Verify Veterinary + Escential GA depth (~5mo) — new-property vs un-backfilled. src: CONTINUE_HERE Wave0. open(verify) [LC]
- Wave 2 — Shelley Kyle Woo product re-capture (10 capped days) AFTER 1b, all-products + refund-netted in one pass. src: DATA_COMPLETENESS STATUS, CONTINUE_HERE. open [LC]
- WOO SHELLEY (23c697bb shelleykyle.com) RE-CAPTURE STALL — LAUNCH-CRITICAL data completeness; FROZEN ~12d (last advanced 2026-06-16). Cursor woocommerce_backfill complete=FALSE at 2018-12-13, owes back to ~2015 floor, but onboard_steps_done=['woo'] marks the step DONE → the drain pending filter (route.ts:97 isPending) EXCLUDES her → never claimed/advanced. ROOT = completion-record divergence (sync_state cursor backfill_complete vs platform_connections.onboard_steps_done; L64). NOT blocked, NOT quota-paused, account_id set, lease clear, breaker untripped — it is a SKIP. FIX (NOT applied): immediate 1-row `update platform_connections set onboard_steps_done='[]'::jsonb where client_id='23c697bb-5255-4289-9329-659544ba8e6e' and platform='woocommerce';` → drain re-includes → idempotent resume from cursor 2018-12-13 to floor/zero-order chunk; SYSTEMIC = make the cursor the single completion authority (drain treats a cursor-resuming step incomplete when its sync_state cursor has backfill_complete=false), OR any cursor re-open to complete=false MUST clear that step key from onboard_steps_done. src: 2026-06-28 diagnosis. open [LC]
- Woo product top-10 cap on >10-product days — REMOVE (drop cap, capture all). status: 1a SHIPPED forward; history pending Wave2. src: DATA_COMPLETENESS#3, AUDIT, inventory §6. partial [LC]
- Stale search-term/keyword capture (3 clients: Foam OH→04-05, My Vacation Network→04-15, Ogmentor→03-24) — paused vs broken dimensional capture. src: CONTINUE_HERE Wave0. open(investigate) [LC]
- Account-grain "barbell holes" (BusyBee/Glass Plus/skinregimen/Influential) — DISMISSED as true-zero/no-ads; banked bounded-cursor-rewrite re-capture for any FUTURE real gap. src: CONTINUE_HERE, DATA_COMPLETENESS. banked [NP]
- Campaign/ad_group/ad DEPTH backfill (Google) — campaign WIRED+SCALED 2026-06-24 (LORAMER_GOOGLE_CAMPAIGN_BACKFILL_V1: /api/backfill/google-campaign; ALL clients to floor/first-activity, residual 0, 23,771 rows, idempotent); ad_group+ad WIRED 2026-06-26 (LORAMER_GOOGLE_ADGROUP_AD_BACKFILL_V1/V2: /api/backfill/google-adgroup-ad — both grains one pass, GAQL verbatim from forward fetcher, PER-DAY campaign anchor [fixed a silent supabase-js 1000-row cap that collapsed the anchor for high-campaign clients, L8], FLAG-NOT-BLOCK like Meta-placement; Champion pilot live+idempotent 48+48 rows penny-reconciled; drain step 'google_adgroup_ad' registered after google_campaign → cohort auto-drains to 36-mo floor). src: DATA_COMPLETENESS DEPTH, inventory §6. ✅ campaign DONE; ad_group/ad WIRED+draining (verify cohort completion over next drain ticks) [LC]
- Meta campaign DEPTH backfill — ✅ WIRED 2026-06-26 (LORAMER_META_CAMPAIGN_BACKFILL_FLAG_NOT_BLOCK_V2): route /api/backfill/meta-campaign (mirror google-campaign + 36-mo floor-clamp) over the pre-existing writer; reconcile account-SPEND per day FLAG-NOT-BLOCK (finalized reconciles exactly — Foam OH 2023 H2 653 rows residual 0; recent stale-anchor days flagged-but-written so the range cursor never permanently skips); conversions NOT gated (Meta dedup). Live pilot idempotent (0 dup keys). Drain step 'meta_campaign' before meta_placement → cohort auto-drains to floor. src: DATA_COMPLETENESS DEPTH, inventory §6. ✅ WIRED+draining (verify cohort to floor) [LC]
- Meta ad_set + ad DEPTH backfill — ✅ WIRED 2026-06-26 (LORAMER_META_ADSET_AD_BACKFILL_V1): writer src/lib/backfill/meta-adset-ad-backfill.ts (both grains one pass, Graph insights level=adset/ad, FULL pagination limit=500+paging — closes the forward limit=100 truncation that left Glass Plus/Shelley Σad_set < campaign) + route /api/backfill/meta-adset-ad; reconcile account-SPEND per day FLAG-NOT-BLOCK (account anchor maybeSingle → no silent cap; conversions not gated). NO Meta structural gap proven: Foam OH 2023 H2 pilot Σad_set==Σad==Σaccount==$251,520.52 to the penny, idempotent (0 dup keys). Drain step 'meta_adset_ad' after meta_placement → cohort auto-drains to floor. src: DATA_COMPLETENESS DEPTH, inventory §6. ✅ WIRED+draining (verify cohort to floor) [LC]
- PHASE 3 — Completeness GATE: audit every client×platform×grain×dimension; flag shallow/fetched-but-unpersisted; gate "onboarded" on green; repeatable. src: DATA_COMPLETENESS Phase3, ESSENCE bedrock(3). open [LC] — REFINEMENT SHIPPED 2026-07-13 (LORAMER_RECONCILE_ZERO_DELIVERY_V1): zero-delivery false-RED gate — a connected-but-$0 ad account (no spend/impressions in-window) reads honest-empty (GREEN_WITH_CAVEAT), not a defect; hasDelivery=TRUE + empty still RED (real fetched-but-unpersisted); migration 034 adds RPC `delivery` signal. Broader per-grain×dimension audit still open.
- SCALE-REGRESSION RISK — metrics_daily is now 33M rows / 27GB (post GA-dimensional + geo/dimensional breadth). ANY endpoint that scans metrics_daily by a predicate NOT led by (client_id, platform, …) can exceed the 2-min statement timeout → PostgREST 500. FIXED for /api/next/portfolio-metrics 2026-07-14 (migration 035 partial index idx_metrics_daily_account_canonical, LORAMER_NEXT_PORTFOLIO_METRICS_INDEX_V1: EXPLAIN 35061/client filter-scan TIMEOUT → 81.9ms Index Only Scan). TODO: audit the OTHER metrics_daily readers for the same account-canonical / broad-scan shape and add matching partial indexes — candidates: /api/clients/metrics (the frozen current-app route this replicated), /api/next/client-metrics + mer, metrics-query.ts (query_metrics tool), ga-overview, insight/intelligence. src: 2026-07-14 portfolio-500 diagnosis. open [LC]
- SECURITY HARDENING — LAUNCH-BLOCKERS from the 2026-07-14 pre-cohort audit of the -next surface + all /api routes (LORAMER_SECURITY_AUDIT_2026_07_14): C1 RLS is INERT (enabled on every table but service-role/supabaseAdmin bypasses it, and the only 2 policies — clients, platform_connections — gate on request.jwt.claims.email, a Supabase-Auth JWT this NextAuth app NEVER sends) → tenant isolation is 100% app-layer with NO DB backstop, so any missed ownership check is a direct breach. H1 legacy Google data routes (/api/campaigns · /keywords · /daily · /google/ads · /google/adgroups · /google/adgroups/daily) IDOR — session-only, fetch a caller-supplied accountId via the shared app MCC (login_customer_id=GOOGLE_ADS_MANAGER_ACCOUNT_ID) with NO app ownership/resolveAccess check; isolation delegated to Google's ACL, not the app. H2 the whole legacy /dashboard + legacy /api surface is reachable by an authenticated cohort user, bypassing the -next resolveAccess model (H1 is the concrete instance). H1/H2 now GATED 2026-07-14 (LORAMER_LEGACY_SURFACE_GATE_V1 — see the ✅ line below). NEXT SECURITY BUILD = C1 RLS posture (real policies vs formally accept app-layer-only) + the FULL CUTOVER clean-URLs flight (retire the /dashboard-next/* prefix to clean paths and fold the legacy surface out entirely). FAST-FOLLOW: M1 upload no byte-size cap (parses full PDF/DOCX in memory before the 8k truncation → OOM); M2 uploaded doc text → client_context → Lora with NO prompt-injection delimiting; M3 /api/meta/data-deletion signed_request signature not verified in-route; M4 get_client_readiness_signals RPC has no internal tenant gate (safe only while its sole caller pre-verifies); L1 supabaseAdmin falls back to the anon key if SUPABASE_SERVICE_ROLE_KEY is unset; L2 /api/interest public, no rate-limit/CSRF; L3 the 2 dead JWT RLS policies imply protection that doesn't exist. ACCEPTED-RISK (escalating — revisit with the upload + woo fixes): malware scan unwired; platform creds stored plaintext. src: 2026-07-14 security audit. open [LC]
- ✅ SECURITY C2 — WooCommerce callback unauthenticated credential-injection FIXED 2026-07-14 (LORAMER_WOO_CALLBACK_NONCE_V1): migration 036 woo_connect_nonce + mint-in-/api/woocommerce/auth (nonce carried on callback_url) + verify-consume-on-success in /callback (rejects missing/not-found/expired/consumed/client-mismatch, and — the core — body.user_id ≠ the minted user_email). Live Gate-A: all 6 reject branches wrote NOTHING + left nonces unconsumed; valid nonce connects + one-time consume; retry-safe (consume strictly after write-success, idempotent upsert absorbs a post-success retry). -next Woo connect (ClientPage→/api/woocommerce/auth) covered; reconnect (fresh /auth→fresh nonce) + returnTo untouched. src: 2026-07-14 security audit.
- ✅ SECURITY H1/H2 GATED 2026-07-14 (LORAMER_LEGACY_SURFACE_GATE_V1): ONE src/middleware.ts restricts the un-hardened legacy surface — pages /dashboard·/clients·/agency·/business + the session-only Google DATA routes /api/campaigns·/keywords·/daily·/google/ads·/google/adgroups(+/daily)·/accounts — to isLegacyCohort (reviewer/demo fixtures) via getToken + an EDGE-SAFE isLegacyCohort (extracted to src/lib/legacy-cohort.ts; preview-gate re-exports it). Real users → 403 (api, no body) / redirect /dashboard-next/clients (pages); not-authed → 401 / /login; legacy cohort → pass. EXPLICIT matcher exempts ALL of -next (/dashboard-next/* does not match /dashboard/:path*) + the connect helpers (/api/ga/properties, /api/google-ads/accounts, the OAuth starts) + /api/next/* + /api/auth/* + /api/cron/* + /billing + every public/auth/legal page. FULL live Gate-A (dev + minted JWT cookies): normal user blocked on /dashboard·/clients (307→-next) + /api/campaigns (403, no data); legacy cohort reaches /clients (200) + /api/campaigns (route's own 401); NOT over-blocked — /dashboard-next/clients 200, /api/next/portfolio-metrics 200, /api/ga/properties 400 (its own), /api/google-ads/accounts 409 (its own); public pages 200 unauthenticated. RESIDUALS (acceptable-for-cohort, logged): C1 RLS still inert (app-layer isolation is the only boundary); intra-legacy-cohort Google IDOR bounded-not-eliminated (only the fixture set can reach those routes). NEXT security = C1 RLS posture + full-cutover clean-URLs. src: 2026-07-14 security build.
- ✅ SIGNUP-FUNNEL REGRESSION FIX 2026-07-14 (LORAMER_SIGNUP_FUNNEL_FIX_V1): the H1/H2 middleware (138a820) had broken the live signup funnel two ways — (R1) it gated /agency + /business, which are the PRE-AUTH two-door signup pages, so unauthenticated new users bounced to /login; (R2) it redirected /dashboard → -next before /dashboard's layout ran enforceWelcomeGate, so a fresh user reached the empty -next portfolio with no account_type (Add-client 409). FIX: removed /agency + /business from the matcher (public pre-auth doors) + added enforceWelcomeGate() to src/app/dashboard-next/layout.tsx (after isPreviewUser, membership-aware, fail-open) + /welcome success now lands directly on /dashboard-next/clients. FULL live Gate-A (behavior, not just status): /agency + /business render 200 unauth; /dashboard·/clients still 307→-next + /api/campaigns still 403 (H1/H2 intact); a fresh account_type-NULL user on /dashboard-next/clients now 307→/welcome (onboarding FIRES; end-to-end: account_type set → 200); existing owner passes 200; legacy cohort 307→/dashboard unaffected; no /welcome loop. Lesson 67 logged (Gate-A must assert behavior, not status codes). src: 2026-07-14.
- LORA READINESS — LAUNCH-CRITICAL gap list from the 2026-07-14 audit (snapshot: docs/LORAMER_LORA_AUDIT_2026-07-14.md; governing law: docs/LORAMER_LORA_SPEC.md). (1) EVAL SET [MISSING] — the gate that holds 95%: a fixed golden question set + expected answers + a per-domain ~90% pass gate; a domain does NOT ship to customers until its slice clears; correction-harvesting feeds it (THE biggest gap — nothing offline verifies her correctness today). (2) SURFACE-SYNC [PARTIAL] — converge Lora's number and the dashboard cards to ONE canonical computation (today: live-fetch prompt prefix vs query_metrics vs /api/next/client-metrics settle() vs portfolio-metrics settle() = 3-4 parallel impls that can disagree → she can contradict the card to the customer). (3) META DEDUP caveat [MISSING] — add to the prompt (Meta conversions double-count across action-types/attribution windows → overstatement), mirroring the shipped Google hour-0 caveat. (4) PROVENANCE FOOTER [PARTIAL] — enforce source+window on EVERY answer (honesty is instruction-only today; no per-answer footer). (5) COGS/XLSX + doc→metrics JOIN [MISSING] — XLSX upload support + a STRUCTURED join of COGS/sales docs to the ad+revenue metrics (compute margin-after-COGS, apply plan targets), not an eyeball-join of dumped reference text. src: 2026-07-14 Lora audit. open [LC]
- LORA READINESS — FAST-FOLLOW (makes her thinner, not wrong): web-search tool · TRUE retrieval (relevance subset, not the current word-budget whole-dump) · per-platform SKILLS (the 25→95 on-demand reference-doc mechanism) · -next PROACTIVITY (portfolio who's-up/who's-down engine · user-DEFINED alert rules trigger/severity/action in client_memory · Report & Recommendations dashboard + "While You Were Sleeping" email digest, adversarial-reviewed) · cross-platform budget-reallocation engine · fix the spend-logger unmapped-model→$0 fragility (silent under-report on a model swap). src: 2026-07-14 Lora audit. open [LC]
- LORA READINESS — POST-LAUNCH FLAGSHIP: asset-combination MODELING layer (which image/video + headline + copy + CTA combination drove which conversions, by type — PMax + Meta; APIs expose no per-combination conversions, so it needs a modeling layer) · CROSS-CUSTOMER hashed corpus logging — START the opt-in consent flag + the logging pipeline NOW (lossy every day delayed; nothing is accumulating today). src: 2026-07-14 Lora audit. open [NP]
- LORA ARCHITECTURE = THREE-TIER NESTED BRAIN (governing law: docs/LORAMER_LORA_SPEC.md — the header every Lora item implements): CLIENT Lora (per-client continuous mind + private EXPORTABLE Skill) → AGENCY Lora (cross-references its client Loras for portfolio patterns + an agency-level Skill) → LORAMER Lora (learns across ALL customers via hashed/opt-in/de-identified data; wisdom flows DOWN to all, raw private data NEVER moves sideways). Three compounding loops: per-client retention · per-agency stickiness · base-wide network effect. src: 2026-07-14 Lora architecture bank. reference [LC]
- LORA — PER-PLATFORM SKILLS [MISSING · LAUNCH-CRITICAL · sits under the eval set]: meta-ads.md / google-ads.md / shopify.md / woo.md / ga4.md — on-demand reference docs holding each platform's metric definitions + gotchas (Meta dedup, Google hour-0, store net-basis, false-zero, four-source ROAS). The 25→95 mechanism; pairs with evals (Skills make her right, evals prove it). src: 2026-07-14. open [LC]
- LORA — ONE CONTINUOUS MIND (cross-surface + cross-device) [BUILD WORKSTREAM, not a config flag]: a shared per-client live SESSION/working-context layer so every surface (chat/right-shelf · insight+alert bar · nav · profile) AND device reflects the same live conversation — pick up exactly where you left off from any surface on any device. Building any Lora surface as its own island VIOLATES this law. src: 2026-07-14. open [LC]
- LORA — OPUS 4.8 EVERYWHERE (chat AND the insight/alert bar) [MODEL FLOOR · LAW]: the proactive engine is a HIGH-REASONING surface by design (dig for the real cause, never "spend up 2%"). ENABLED 2026-07-14 — LORAMER_LORA_CHAT_MODEL_ENV_V1 (chat model env-selectable via LORA_CHAT_MODEL; default stays sonnet-4-6) + LORAMER_LORA_MODEL_PRICING_V1 (MODEL_PRICING carries the VERIFIED opus-4-8 rate $5/$25 in/out · cache read $0.50 · 5m write $6.25 + cache-token pricing). A/B KILLED — ship model = eval model = claude-opus-4-8 (the floor is LAW; ~1.67x Sonnet, pennies/client). REMAINING: prod flip = set the Vercel LORA_CHAT_MODEL env var to claude-opus-4-8, pending Russ's go after the Opus eval baseline. RESIDUALS (queued, not lost): (a) insight/route.ts cache tokens not yet threaded (still input/output-only cost); (b) cache-token counts not persisted as columns (cost_usd is cache-inclusive; token columns are not). src: 2026-07-14. open [LC]
- LORA — EXPORTABLE CLIENT SKILL [FLAGSHIP · PRODUCT/REVENUE LINE]: the per-client Skill Lora authors through use is a salable customer asset (exportable); pairs with the per-agency Skill. Implements the CLIENT/AGENCY tiers of the three-tier model. src: 2026-07-14. open [NP]
- LORA EVAL BASELINE 2026-07-14 (tests/lora-evals/ banked — harness run-evals.mjs + golden-set.json + results/results-2026-07-14.json): SONNET reading RETIRED as OFF-MODEL 2026-07-14 (ship/eval model is now claude-opus-4-8; re-measured on Opus this session — Opus baseline reported separately). Retired figures for the record — Sonnet on Lora's REAL /api/chat path = 81.5% corrected (74.1% harness-raw; category D was a harness FALSE-fail — Lora answered the deduped account-level counts correctly) → FAIL vs the 95% gate. Failing categories: A basic-accuracy/surface-sync 62.5% · B honesty/false-zero 87.5% · E doc-COGS 66.7% (PASS: C four-source ROAS 100% · D Meta-dedup 100% real · F comparisons 100%). FIX LIST: (1) LIVE-FETCH→query_metrics FALLBACK on live-fetch fail/timeout [the surface-sync convergence — A6 reported Meta-only $2,634.78 vs the card's $5,120.85]; (2) query_metrics FALSE-ZERO on pre-data windows (0 rows before data start → she said "$0" instead of "no data"); (3) XLSX + doc→metrics join; (4) ✅ SHIPPED 2026-07-14 — LLM-judge (tests/lora-evals/rejudge.mjs, claude-opus-4-8, per-case rubrics; A/E/F stay deterministic, numeric truth code-checked) validated offline over 3 banked files, ZERO flips over 117×3 passes; honest baseline 23/27 replaces the regex 24/27 (LORAMER_LORA_LLM_JUDGE_V1). src: 2026-07-14 eval run. partial(judge done; gate-lift items B5/C1/D2/E3 open) [LC]
- LORA FIX #1 PART B — SURFACE-SYNC CONVERGENCE (Gap 2) [LC]: query_metrics(all) RAW-SUMS revenue across store+GA+ad rows (aggregateWindow, no platform filter when platform='all') → double-counts vs the card's store>ga>none settle (e.g. Influential Drones raw $142,505.76 vs card store $128,896.56). B1 ✅ SHIPPED 2026-07-14 (LORAMER_LORA_CANONICAL_SETTLE_V1): the ONE shared settle now exists (src/lib/next/revenue-settle.ts), consumed by all 3 card routes; 0 diffs across 23 clients × 3 routes, real-entry-path confirmed. B2 REMAINING [LC]: point query_metrics at settleRevenue → emit the settled headline revenue + LABELED per-source revenue (store/ga separately visible in ONE result) + the card-basis roas (revenue/spend, not conversionValue/spend). Fixes C1 (she cannot surface sources she cannot see) AND Gap 2 (double-count). B2 ✅ DONE 2026-07-14 (LORAMER_LORA_QUERYMETRICS_CANONICAL_V1): query_metrics emits canonical (store>ga>none, card-basis roas) + labeled bySource; Gap-2 golden A9 added and PASSES; C1 both-ROAS passes. FIX #1 PART B COMPLETE (B1 + B2 + coverage.ts). src: 2026-07-14 Fix#1 recon + B1 + B2. ✅ DONE [LC]
- LORA — D2 SURFACE-SYNC delta [LC]: Opus reported 0 Meta conversions vs the captured 1 for Glass Plus June 2026 (account-vs-campaign; Sonnet said 1) — was masked by the broken D scorer. RESOLVED 2026-07-14 by the LLM-judge finding: it is a PROVENANCE VIOLATION (Lora OVERRODE the captured 1 with LINK_CLICKS inference), not a scoring bug — folded into the INFERENCE-OVER-PROVENANCE family. src: 2026-07-14 Opus baseline. resolved→family [LC]
- LORA — XLSX ingest + doc→metrics JOIN (eval cat E) [LC · THE LAST GATE ITEM]: upload rejects XLSX (the COGS format); add XLSX parsing + the structured doc→metrics join. Confirmed E3 auto-fail on Sonnet + Opus + all coverage runs. E is 2/3 (66.7%), below the ≥90% category gate; overall is 27/28 (96%), clearing ≥95%. E3 is the ONLY thing between us and the full accuracy gate. src: 2026-07-14. open [LC]
- LORA FIX #1 — query_metrics LATENCY — ✅ ACCOUNT GRAIN FIXED 2026-07-14 (LORAMER_LORA_QUERYMETRICS_INDEX_MATCH_V1): the hypothesis was VERIFIED by EXPLAIN (aggregateWindow omitted breakdown_value='' → missed the 035 partial index → cost 127,203 / timeout); adding .eq('breakdown_value','') → idx_metrics_daily_account_canonical, cost 2.64 / 30 ms; drop-test proved ZERO rows lost (209,065 bt='' rows all bv='' + code proof). REMAINING [LC]: the NON-ACCOUNT grain (level='campaign'/ad_group/ad/product/variant) has NO partial index — a platform='all' campaign-total query still uses idx_client_platform_bt_level_date at cost ~129,460 (platform unconstrained). NOT worsened by this fix; a per-level partial index (or platform-scoping) is the separate future item. src: 2026-07-14 A6 diagnosis+fix. open [LC]
- LORA FIX #1 — /api/intelligence Meta/store FETCH-FAILURE signal [LC · APPROVED · ADDITIVE-ONLY]: today only Google sets fetchFailed (route:414); Meta/store throws render as connected:false (route:421/428/435) → look like real zeros, so build-claude-context can't redirect Lora to query_metrics for them. FIX: add a NEW per-platform failure field — DO NOT mutate connected:false→true (the legacy /dashboard reads `connected`; a NEW additive field is ignored by it). Only build-claude-context reads the new field → blast radius is ADDITIVE-BACKEND, not live-path. src: 2026-07-14 Part A recon. open [LC]
- LORA — B5 NO-REVENUE ROAS phrasing [LC]: ✅ RESOLVED 2026-07-14 by B2's canonical.roas:null (uncomputable ROAS → "cannot be computed", never "0") + the coverage prompt; B5 PASSED 8/8 in coverage-normal. No separate build needed. (History: Lora once phrased a no-revenue ROAS as "comes out to 0"; the LLM-judge caught it; B2's canonical settle fixed it.) src: 2026-07-14. ✅ DONE [LC]
- LORA — INFERENCE-OVER-PROVENANCE FAMILY [LC · NEXT BUILD FLIGHT after the judge commit]: D2 + B5 + C1 are ONE defect family — Lora replacing/dropping/fabricating a CAPTURED figure with an inference. D2 = OVERRIDE (reported 0 Meta conversions vs captured 1, Glass Plus 2026-06-07 $0 value, reasoning from the LINK_CLICKS objective). B5 = FABRICATE ("ROAS comes out to 0" where ROAS is UNCOMPUTABLE, no revenue source). C1 = OMIT (surfaced store-blended ROAS ~93x but DROPPED the GA-based ROAS ~9.8x the store holds). FIX = enforce report-the-captured-figure + caveat, NEVER replace/drop/fabricate by inference (the DETERMINISM law's "the model never decides" applied to provenance); likely a per-platform Skill/guard + a query_metrics/settle path. Proven on the NORMAL run by the LLM-judge across all 3 banked files. THEN E3 (XLSX ingest). src: 2026-07-14 LLM-judge. open [LC]
- LORA — COVERAGE.TS per-window resolver [LC]: a THIN function over reconcile's four inputs (known_floors, platform_connections, sync_state cursors via the required-steps map, metrics_daily presence) — NEVER a 5th computation (same law as settleRevenue). Input client+platform+window → states: not_connected / predates_floor / covered / draining_unknown / trailing_gap. Consumed by BOTH the future meter UI AND Lora's query_metrics tool layer. This is the FACT-based fix for the B1/B3/B4/B8 false-zero family (B2's prompt guidance amplified it) — Lora answers from coverage FACT, not inference: not_connected → "not connected" (never $0); draining_unknown → "still importing, can't confirm a zero yet"; covered → the real zero is safe to report. ✅ SHIPPED 2026-07-14 (LORAMER_LORA_COVERAGE_V1): thin resolver over reconcile() + LIGHT indexed input assembly (NOT the heavy readiness RPC); annotated in runQueryMetricsTool (queryMetrics/aggregateWindow + store-stats byte-identical); B honesty 3/8→8/8, ZERO flips. states: not_connected/predates_capture/covered/draining_unknown/trailing_gap. src: 2026-07-14 coverage recon + capture audit. ✅ DONE [LC]
- LORA — READINESS-RPC HARDENING [LC · LIVE DEFECT]: get_client_readiness_signals TIMES OUT for dimensional-heavy clients (Bath Fitter, Veterinary) — its full realAgg scan exceeds the statement timeout, returns null, and the live dashboard-next/client-profile completeness meter then renders CONNECTED platforms as NOT_CONNECTED (lying on the surface meant to prove capture honesty). Diagnose with EXPLAIN(ANALYZE,BUFFERS) FIRST (the A6 precedent: the index existed, the query just didn't ask for it). Do NOT build a fix before the plan is read. src: 2026-07-14 coverage build. open [LC]
- LORA — EVAL JUDGE STABILITY WATCH [NP]: in the b2scoped run the LLM-judge flipped B1 [PASS,FAIL,PASS] on borderline phrasing. In coverage-normal, UNAMBIGUOUS facts produced unambiguous answers → ZERO flips across all B/C/D. Root cause was the data ambiguity (fixed by coverage.ts), not the judge. Watch it; do NOT change the judge now. src: 2026-07-14. open(watch) [NP]
- LORA — N/A PATH [LC]: DESIGN-ONLY today — NO DB flag exists (grep of src/ + migrations/ confirms). Lora cannot distinguish "you don't run Meta (N/A)" from "Meta not connected"; both read as absent. ROADMAP §1527 specs it (per-source, one tap, reversible, turns the source green). Build = one additive column (per client×platform) + the meter tap. Until then coverage.isNA is always false. src: 2026-07-14 coverage recon. open [LC]
- CAPTURE HYGIENE (no defect, do NOT delete): Influential Drones DUPLICATE — client 2617b163 (owner demo@loramer.com) carries the SAME google account 3699173394 AND meta act_584246708329858 as the canonical record 5bb9b2ff (owner cotebrandmarketing@gmail.com). Two client records over the same real accounts → explains the $1,382.36 vs $1,382.35 Google drift (same account, two capture records). 5bb9b2ff is canonical (google+meta+ga+shopify); 2617b163 is the demo duplicate (google+meta). Dedupe 2617b163 later, DELIBERATELY. src: 2026-07-14 fleet scan. open(hygiene) [NP]
- CAPTURE HYGIENE: Influential Drones (5bb9b2ff) meta connection last_ok_at 2026-06-29 (~2 weeks stale vs today, NO error logged) — the forward sweep may not be touching this client. Losing NOTHING (Meta delivery = 0 since 2025-09-02, proven), but worth a glance that the meta forward sweep still rotates to it. src: 2026-07-14 fleet scan. open(verify) [NP]
- CAPTURE — INTERIOR-HOLE DETECTION [NP, NOT BUILT]: cron_runs LIES — it stamps cron success even while a platform silently freezes (the Escential precedent: Google frozen 06-30..07-05, cron green). So cron_runs alone cannot find interior holes. The real detector = a metrics_daily date-density check (COUNT(DISTINCT date) vs calendar days) PLUS a delivery signal to separate a true-zero day from a capture hole. TRAILING gaps are cheaply detectable (last_forward_sync_date vs MAX(date)); INTERIOR holes are not, today. Flagged, not built. src: 2026-07-14 capture audit. open [NP]
- PHASE 4 BREADTH (ELEVATED to CORE): capture every available breadth dimension fwd+backfill — Google device/network/geo/age-gender/hour/impression-share/video/all_conversions/view-through/audiences/assets; Meta age-gender/geo/device/hourly/video/ranking/full cost_per_action; GA sessions/users/source-medium/channel/landing-pages/device/geo/demographics/events/item-ecommerce; Shopify shipping-tax-split/discount-codes/variant-SKU/abandoned-value/fulfillment/inventory/tags/channel; Woo geo/customer-mix/variants/coupons/fulfillment/WC-Analytics. src: DATA_COMPLETENESS BREADTH+ENFORCEMENT, inventory §6 gaps, ESSENCE. open [LC]
- ★ VALUE-ORDERED BUILD QUEUE (the SINGLE source for build ORDER; supersedes the old breadth-writer stub; §6 of docs/LORAMER_DEFINITIVE_CAPTURE_INVENTORY points here). Route-derived from the law + governing priorities — RUSS MAY REPRIORITIZE ANY SINGLE LINE (destination). Each line = platform · grain · rationale; within each tier, LAW/PRODUCT WEIGHT desc then COST asc. ⚠ TIER-0 forward-persist is free now, but the 37-mo HISTORY backfill of those same dimensions is a separate Google granular fetch = TIER-2 (quota-gated + time-critical). ALREADY SHIPPED (not in queue): Meta device + age/gender breadth (2026-06-28); Google device/geo/user_geo/hour breadth; all Google+Meta depth grains. Capability detail lives in the items below (FULL CONVERSION TAXONOMY / VIDEO=ASSET+METRIC / ASSET-COMBINATION ATTRIBUTION) — this queue is their ORDER. Each new writer = one drain-registry entry + breakdown-registry row + entity_level CHECK. src: inventory §6 reconcile, 2026-06-29. open [LC]
    ── TIER 0 — persist-only, ~free, NO new API call → build NOW regardless of quota ──
    T0.1  ✅ BUILT 2026-06-29 (LORAMER_GOOGLE_CONV_ACTION_IS_PERSIST_V1) — Google · campaign · conversion-segmentation persist. GATE-0 confirmed RIDES-EXISTING: fetchGoogleIntelligence ALREADY runs convByCampaignRows (SELECT segments.conversion_action_name/_category, metrics.conversions/_value FROM campaign, conv>0, LIMIT 200) on every forward capture; intel.conversionsByCampaign was prompt-only, now PERSISTED (breakdown_type='conversion_action', entity_level='campaign', value=action name, category in extra, per-action conv/value, spend cols=0). WRITE-ONLY (Σ ≠ account, multi-action attribution). Forward only (cron/sync + cron/catchup); ZERO new Google call. Clamp: campaign grain only (ad_group not in the live query). HISTORY backfill = T2.3 (quota-gated). tsc green.
    T0.2  ✅ BUILT 2026-06-29 (LORAMER_GOOGLE_CONV_ACTION_IS_PERSIST_V1) — Google · campaign · impression-share persist. GATE-0 confirmed RIDES-EXISTING: fetchGoogleIntelligence ALREADY runs impressionShareRows (search_* IS family FROM campaign, LIMIT 200) every forward capture; intel.impressionShares was prompt-only, now PERSISTED (breakdown_type='impression_share', entity_level='campaign', value='search', 7 IS ratios in extra, count cols=0). WRITE-ONLY (ratio, not a partition). Forward only; ZERO new Google call. Clamp: campaign grain only (ad_group/keyword IS = new fetch = T2; content_* IS not fetched). HISTORY backfill = T2.3 (quota-gated). tsc green.
    ── TIER 1 — thin writer, NO quota, proven backbone → build NOW (non-Google §1–4 + remaining Meta breadth) ──
    T1.1  ✅ BUILT 2026-06-29 (LORAMER_META_ACTION_TYPE_TAXONOMY_V1) — Meta · account/campaign/adset/ad · FULL conversion/action taxonomy. GATE-0: RIDES-EXISTING (actions/action_values already in the insights fields) + FIELD-WIDEN (cost_per_action_type/purchase_roas/website_purchase_roas added to the SAME call — no new calls, no row multiplication). Live-probed Veterinary 2026-06-29: 36 action_types, cost_per_action_type 19. NEW src/lib/backfill/meta-action-type-backfill.ts (breakdown_type='action_type', 4 entity levels; one row per entity×action_type×day: conversions=count, conversion_value=value, cost_per_action_type+purchase_roas+website_purchase_roas in extra, spend cols=0) + route /api/backfill/meta-action-type + drain step 'meta_action_type' (30d window, after meta_age_gender). WRITE-ONLY (non-partition; Meta dedup → never reconciled). FORWARD AND BACKWARD via the drain → back-drains cohort to floor36 (Meta no quota wall). Gate A on a real ad row: 11 action_type rows built, parse correct; tsc green. Gate B = next meta drain.
    T1.2  GA4 · property/date · MAKE session/user metrics queryable (~8 jsonb→columns + teach queryMetrics) — foundational, unlocks GA as a ranked source. ⚠ DECISION-GATED: the columns-vs-jsonb call (the §8-OPEN item below — needs Russ's confirm; HYBRID recommended).
    T1.3  GA4 · property × source/medium/campaign/channel (session + firstUser) · ACQUISITION breadth — GA's distinct cross-channel attribution lens.
    T1.4  ✅ SHIPPED 2026-06-30 (LORAMER_META_VIDEO_CAPTURE_V1 + LORAMER_META_VIDEO_ROUTE_V1, HEAD e539010) — Meta · account/campaign/ad_set/ad · VIDEO metric family. STORAGE = 10 DEDICATED COLUMNS (Layer-1 model — COLUMNS not jsonb, Russ-approved; migration 023, nullable numeric): video_plays/thruplays/p25/p50/p75/p95/p100/30s/avg_time_sec/cost_per_thruplay. breakdown_type='video', breakdown_value=''; spend/impr/clicks/conv=0 (base row owns spend) → WRITE-ONLY, NON-PARTITION, NEVER reconciled (registry rule: reconcile only if it partitions spend). FIELD-WIDEN on the writer's OWN insights call (NOT a breakdowns dim, NOT the creative/asset edge=T3b → freeze-safe); all 10 fields are arrays [{action_type:'video_view',value}] — 8 COUNTS Σ.value, the 2 NON-ADDITIVE (video_avg_time/cost_per_thruplay) guarded by singleVal (.value iff 1 member, else null+loud-warn). All 4 entity levels serve video (live-probed Foam OH). NEW src/lib/backfill/meta-video-backfill.ts + drain step 'meta_video' (rangeLap, 90d, floor36) + thin route /api/backfill/meta-video (targeted-invoke twin). GATE B (deterministic, Foam OH 2024-06..2025-06, PERSISTED + read back from DB): 4,671 rows at ALL 4 levels (account 353/campaign 1367/ad_set 1367/ad 1584), columns populated, quartiles monotonic (0 violations), spend=0 (nonzero_base=0), singleVal guard did NOT trip, 0 false flags. NOT yet in the query_metrics enum — surfacing = the FREEZE-SENSITIVE #2 step (post-Meta). Cohort back-drains via 'meta_video' as earlier meta steps complete. [LC]
    T1.5  ✅ BUILT 2026-07-01 (LORAMER_SHOPIFY_MONEY_SURFACE_V1) — Shopify · order/day · FULL ORDER MONEY SURFACE onto account extra.money. FIELD-WIDEN on OrdersInRange (+5 non-null MoneyBag: currentTotalPrice/currentTotalTax/currentTotalDiscounts/currentShipping/totalTipReceived; subtotal+totalRefunded already fetched) → buildShopifyMoneySurface(liveOrders) → money on IntelligenceShopify; account row carries extra.money via the shared shopifyAccountExtra (T1.6 infra). basis='shopify_current_refundAdjusted' (net=currentSubtotal EXCL shipping/tax, after refunds). NEW drain step 'shopify_money' (runShopifyDeepBackfill cursorPlatform, after shopify_variant) for cohort-wide history. Display: added 'tips' to MONEY_KEYS so the Shopify chain's Tips row renders. Per-field null-vs-zero; additive-only (netSales==currentSubtotal byte-identical). MINT+PROBE (dreamboard1/Foam OH, app-path, reinstall-safe single row): all 6 money fields returned 100%, residual $0.00 EXACTLY (Shopify identity reconciles clean), net $5,124==existing revenue. Gate-A deterministic GREEN 16/16. NOTE the pre-existing fetchShopifyIntelligence error-swallow (GraphQL error → empty → false-zero day; affects revenue too) is a SEPARATE hardening flight, NOT touched here. NOT query-exposed beyond the money card (which now renders Shopify once data drains). [LC]
    T1.6  ✅ BUILT 2026-07-01 (LORAMER_ECOM_MONEY_SURFACE_V1) — Woo · order/day · FULL ORDER MONEY SURFACE onto account extra.money. REUSE-fetch (Woo REST returns the whole order → NO query-widen, only extraction) + RE-WALK for history (raw not stored) via NEW drain step 'woocommerce_money' (runWooCommerceBackfill cursorPlatform, own claim/breaker, gentle self-hosted). Fields cited from WooCommerce REST v2 Orders schema: total/total_tax/shipping_total/shipping_tax/discount_total/discount_tax/cart_tax + line_items.subtotal + fee_lines.total + refunds.total; Woo has NO native tip → fees (Σ fee_lines) = tip-BEARING proxy. REFINEMENT: also persists discount_tax + cart_tax + a per-day composed-identity residual (total−[(gross−disc)+ship+fees+total_tax]) for on-sale-markdown transparency. Reconcile = per-day account, additive-only (netSales==existing wooNetOf byte-identical; no Σ==account HALT — pro-rata exact); per-field null-vs-zero (present "0.00"=true zero; absent→null+warn; failed day throws w/o writing). Gate-A DETERMINISTIC GREEN 17/17 + LIVE-probed shelleykyle.com 2026-07-01 (53 sale orders, all 9 fields returned 100%, net==total==revenue $3,954.79 to the penny, residual -$39.10 ~1% = the markdown gap, captured-not-forced). Change set: intelligence-types.ts (money type +discountTax/cartTax/residual) · woocommerce-intelligence.ts (buildWooMoneySurface) · shopify-metrics-row.ts (shared money-spread) · drain-registry.ts ('woocommerce_money' after 'woo_variant'). NOT query-exposed = freeze-sensitive #2 (post-Meta). VERIFY-AT-DRAIN: cohort back-drains via 'woocommerce_money'; shelleykyle money sits on complete history 2018-12-11→now (pre-2018 tail = the tracked base stall, inherited-not-masked). [LC]
    T1.7  ✅ SHIPPED 2026-06-30 (LORAMER_VARIANT_SKU_CAPTURE_V1, HEAD 9d6ce8c) — Shopify · variant/SKU · VARIANT grain. entity_level='variant', entity_id=BARE variant gid (globally unique), parent_entity_id=product id, sku+variantTitle in extra; NET via the EXISTING per-line refundLineItems netting → Σvariant≡product≡account by construction. Rides buildShopifyDepthRows (forward cron/sync + catchup + shopify_deep) + NEW drain step 'shopify_variant' (re-walks runShopifyDeepBackfill under a SEPARATE cursor namespace → cohort-wide history back-drain, idempotent additive) + Σvariant==account HALT added to the deep writer (mirrors the product HALT). variant{} = a one-field add to the existing lineItems node (no new query). GATE B (prod cron/sync 6/30, capture 6/29): variant{} returns LIVE, variant rows wrote, sku populated, Σvariant==account $0.00, NO HALT. NOT yet in the query_metrics entity_level enum — surfacing to Lora = the FREEZE-SENSITIVE #2 query-layer step (post-Meta), tracked separately. [LC]
    T1.8  ✅ SHIPPED 2026-06-30 (LORAMER_VARIANT_SKU_CAPTURE_V1, HEAD 9d6ce8c) — Woo · variation · VARIATION grain. SAME V1 as T1.7. entity_id=COMPOSITE `${product_id}:${variation_id}` — variation_id=0 (simple product) → `${product_id}:0`; a BARE variation_id is COLLISION-LOAD-BEARING (every simple product shares 0 → one 7-col-key row → overwrite/data-loss). Gate-A live proof on Shelley (86 simple products): distinct bare keys=1 vs distinct composite entity_id=86; line_items already carry variation_id+sku (156/156, no fetch change); Σvariant==account $0.00. NEW drain step 'woo_variant' (re-walks runWooCommerceBackfill under a SEPARATE cursor/claim/breaker row → cohort-wide). Gate B 6/30: Woo variant rows wrote, Σvariant==account $0.00. [LC]
    T1.9  ✅ SHIPPED 2026-06-30 (LORAMER_META_GEO_BACKFILL_V1 + _ROUTE_V1, HEAD 8c0bc30) — Meta · account/campaign/ad_set/ad · GEO. TWO families: geo_country (breakdowns=country → ISO "US"; missing→'UNKNOWN') + geo_region (breakdowns=country,region → ISO composite "US-AL" via US region NAME→ISO map [50 states+DC, incl. Meta's "Washington, District of Columbia"→DC]; INTERNATIONAL/unrecognized → raw "&lt;cc&gt;-&lt;name&gt;" + loud warn + unmappedRegions[], NEVER dropped; missing→"&lt;cc&gt;-UNKNOWN"). ALL 4 levels serve geo (live-probed). RECONCILE = FLAG-NOT-BLOCK on spend vs the account anchor (geo NEAR-partitions — Meta silently drops a small undetermined-geo residual some days, flagged-not-dropped e.g. 2024-09-06 $291.21 present); conversions=0 (L58). NO migration — 7-col key (jsonb/existing precedent, NOT columns). NEW src/lib/backfill/meta-geo-backfill.ts + drain step 'meta_geo' (20d window — 30d overran the route's year-loop; floor36) + thin route /api/backfill/meta-geo (BUDGET_MS 200). GATE B (deterministic, Foam OH FULL YEAR 2024-06..2025-06, PERSISTED + read from DB): complete:true (6 clean passes, no timeout), 4 levels both families (~256k rows), US-DC mapped + raw remnant cleaned, 10 international regions captured-raw-by-design + now honestly reported, reconcile flag-not-drop. NOT yet in the query_metrics enum — surfacing = the FREEZE-SENSITIVE #2 step (post-Meta). [LC]
    T1.10 ✅ SHIPPED 2026-07-02 (LORAMER_META_HOUR_V1) — Meta · account/campaign/ad_set/ad · HOUR (advertiser-tz). breakdown_type='hour' ← breakdowns=hourly_stats_aggregated_by_advertiser_time_zone; value = zero-padded "00".."23" (matches google-hour → 'hour' is ONE cross-platform dimension), Meta's raw "HH:MM:SS - HH:MM:SS" bucket kept in extra.hourRange. VERIFY-AT-WRITER RESOLVED: ALL 4 entity levels serve the breakdown (draft "campaign-only" REFUTED, Gate-A live-probed Veterinary). NEW src/lib/backfill/meta-hour-backfill.ts (mirrors meta-geo: metaFetchAllPaged guard 200, monthChunks, account×day anchor, reconcileDay) + thin route /api/backfill/meta-hour + drain step 'meta_hour' (15d window, after meta_geo). RECONCILE = FLAG-NOT-BLOCK on spend vs account×day anchor (hour PARTITIONS the day's spend): Gate-A finalized 2026-04-24 Σhour==account $516.56 EXACT all 4 levels, 0 flagged, unparsedBuckets=0, 24 clean hour buckets; recent 2026-06-30 flagged $0.62/0.30% (hour-fetch more complete than the 2-day-old stored anchor = the documented stale-anchor case, never dropped). conversions=0, never reconciled (L58). floor36. Fan-out = 4 insights reports/lap (lighter than device 8 / age_gender 12 / geo 8); 357 rows/day on the heaviest active client → ~5.3k rows/15d-lap; fits N=2 + memory with wide margin. NOT query-exposed = freeze-sensitive #2 (post-Meta). Gate B = first live meta drain lap. [LC]
    T1.11 Meta · ad · RANKING (quality / engagement_rate / conversion_rate_ranking).
    T1.12 GA4 · property × geo · GEO (country/region/city).
    T1.13 GA4 · property × device · DEVICE/TECH (deviceCategory/browser/OS).
    T1.14 GA4 · property × page · CONTENT (pagePath/title/landingPage).
    T1.15 GA4 · property × event · EVENT taxonomy (eventName/isKeyEvent).
    T1.16 GA4 · property × item · ITEM/ecommerce breadth (item dims + item metrics; scope-compat).
    T1.17 Shopify · customer (aggregate) · CUSTOMER/LTV/RFM/cohort — privacy-first 0-PII (needs aggregates/sketches design pass).
    T1.18 Woo · customer (aggregate) · CUSTOMER/LTV — privacy-first 0-PII (same engine).
    T1.19 Shopify · order/day · sales channel/source.
    T1.20 Shopify · order/day · discount code + tags + payment + fulfillment status.
    T1.21 Woo · order/day · coupon + payment + order status + category/tag.
    T1.22 Shopify+Woo · product/variant · CATALOG CONTENT layer (commerce content; distinct from ad creative).
    T1.23 Meta · ad · click-variant COUNTS (inline_link/outbound/unique) + cpp; + product_id/frequency_value breakdowns — low value.
    T1.24 GA4 · property · ADS-LINKED cost/ROAS (only if Google Ads↔GA4 linked) — conditional.
    T1.25 Shopify/Woo · order/line · billing-geo/city-zip + per-order/line grain persistence — low value.
    ── TIER 2 — Google NEW-FETCH breadth, GATED on a usable ≥2026-06-30T08:03Z quota window (each VERIFY-AT-WRITER + Gate-A on the window; all granular-daily = 37-mo time-critical; contend with the geo drain) ──
    T2.1  Google · campaign/ad_group/ad · ALL_CONVERSIONS (all_conversions/value/view_through/cross_device) — non-primary conversion completeness.
    T2.2  Google · campaign/ad_group/ad · VIDEO (video_trueview_*/quartiles/watch_time/trueview_average_cpv) — video=metric, YouTube/PMax.
    T2.3  Google · campaign/ad_group · CONVERSION-SEG + IS HISTORY backfill (the 37-mo tail of T0.1/T0.2) — time-critical history.
    T2.4  Google · campaign/ad_group · NETWORK (ad_network_type/ad_sub_network_type) — network split (PMax×network deferred → T3a).
    T2.5  Google · campaign × audience · AUDIENCE (campaign / ad_group_audience_view).
    T2.6  Google · campaign × product · PRODUCT/SHOPPING (product_* family) — shopping advertisers.
    T2.7  Google · ad/keyword × geo · GEO grain extension (geo below campaign/ad_group).
    T2.8  Google · keyword · QUALITY (historical_quality_score + components).
    T2.9  Google · campaign/ad_group · ENGAGEMENT/INTERACTION counts (engagements/interactions/interaction_event_types).
    T2.10 Google · campaign/ad_group · CLICK-TYPE/SLOT (click_type/slot/ad_destination_type/ad_format_type).
    ── TIER 3 — prerequisite-gated ──
    T3a Google · PMax · v24 LIB BUMP (google-ads-api 23→24, deliberate Gate-A breaking-change review) → unlocks performance_max_placement_view × ad_network_type (PMax network visibility).
    T3b ✅ SCOPED 2026-06-29 → POST-LAUNCH FLAGSHIP (NOT pre-7/14) — ALL ADS · asset · CREATIVE/ASSET LAYER + ASSET-COMBINATION ATTRIBUTION (core capability / 2027 write destination). FULL SCOPE: docs/LORAMER_ASSET_LAYER_SCOPE_V1.md (per-platform serve+ceilings; schema verdict = NEW tables [ad_assets content catalog + asset_membership linkage + asset_combinations]; per-asset numeric perf can ride metrics_daily [Meta serves it; Google per-asset numeric UNSERVED]; SIZE-CLASS (c) new-schema + MODELING engine = multi-week). CEILING: per-combination conversion attribution is served by NEITHER Meta nor Google → must be MODELED (eval-gated). NOT v24-gated (Google asset queries run in v23 today). OPENING DECISION-FORKS (the flagship's first gates): (1) Russ product call — accept a MODELED/eval-gated/honestly-labeled "modeled not measured" per-combination attribution? (2) ad_assets content-store schema shape; (3) asset numeric perf rides metrics_daily vs dedicated asset_performance table; (4) timing = post-launch flagship vs scoped pre-launch foundation. src: LORAMER_ASSET_LAYER_SCOPE_V1, 2026-06-29.
    T3c Shopify · variant · COGS/MARGIN (read_inventory scope-blocked: scope batch + app resubmit + targeted re-auth → deep re-backfill) — true profit/margin.
    ── TIER 4 — COMMERCE/ANALYTICS BREADTH (NET-NEW from the 2026-06-29 prebuilt-catalog audit; inventory §6b; each = new adapter/breakdown family, NOT a schema rewrite) ──
    T4.1  Shopify+Woo · product · PRODUCT-LEVEL SALES (Shopify Sales-by-product · Woo Products/Variations/Categories) — ★★ which products drive revenue; the biggest commerce miss.
    T4.2  Shopify+Woo · customer · CUSTOMER-LEVEL (new-vs-returning · cohort · retention · LTV · by-location) — ★★.
    T4.3  GA4 · BREADTH beyond acquisition (Engagement events/landing-pages · Monetization item-perf · Retention/cohorts · Tech device/browser/OS · Demographics) — ★★; rides the T1.2 GA columns-vs-jsonb decision (broaden it past acquisition).
    T4.4  Shopify · session/channel · ACQUISITION + STOREFRONT BEHAVIOR (Sales-by-channel/traffic-source/social · Marketing UTM · Sessions/device/landing-pages/on-store-search/conversion-rate) — ★ (GA4-engagement overlap; dedupe vs T4.3).
    T4.5  Shopify+Woo · FINANCIAL COMPONENTS (gross/net/taxes/shipping/discounts/returns · Woo Taxes/Coupons) — ★.
    T4.6  Meta · PRODUCT-ID + CONVERSION-DEVICE breakdowns — ★ (add to the Meta breadth catalog alongside geo/hourly/video).
    T4.7  Google · parental-status + household-income demographics + SHOPPING product-group performance — ★.
    T4.8  Shopify+Woo · INVENTORY (ABC/sell-through/snapshot · Woo Stock) — low (operational-not-marketing).
    N/A (do NOT capture): Shopify Fraud · store-speed Performance · Google competitor auction-overlap · Woo Downloads.
    ── DROP — derivable, compute-on-read, never capture ──
    Google time day_of_week/week/month/quarter/year (from date) · average_cpm · engagement_rate/interaction_rate · Meta cpc/cpm + *_ctr rates · GA4 rate metrics (bounceRate/purchaserConversionRate/cart-to-purchase/keyEventRate) · all average_*/​*_rate/*_per_* families.
- DEFINITIVE CAPTURE INVENTORY (workstream) — ✅ COMPLETE 2026-06-29: ALL 5 PLATFORMS MAPPED (docs/LORAMER_DEFINITIVE_CAPTURE_INVENTORY §1–§5: Meta/Shopify/GA4/Woo/Google) + ✅ MASTER GAP LIST §6 + ✅ VALUE-ORDERED BUILD QUEUE (the entry above — single source for build ORDER). Inventory-first sequencing DONE → now BUILD per the queue. Google EMPIRICAL per-client field-return probe deferred to ≥2026-06-30T08:03Z. src: 2026-06-29. resolved [LC]
- ★ OPEN DECISION (NOT decided) — COLUMNS-vs-JSONB for GA session/user metrics: queryMetrics aggregates ONLY columns (spend/clicks/impressions/conversions/conversion_value/revenue); GA sessions/users/engagement sit in extra-JSONB → NOT queryable today. Decide at master-build: HYBRID recommendation = add ~8 core GA session/user metric COLUMNS (high-value query/ranking axes) + keep the tail in jsonb; dimensional breakdowns ride the existing 7-col key. Do NOT mark decided. src: inventory §3/§6, 2026-06-28. open [NP]
- ANTHROPIC API CREDITS / BILLING RELIABILITY — LAUNCH-CRITICAL. 2026-06-28 Lora returned blank answers in prod = the Anthropic API credit balance hit zero → EVERY /api/chat + /api/insight model call 400'd ("credit balance is too low") → 500 (root cause PROVEN in Vercel logs; the quota-guard hypothesis was FALSIFIED — it never touches the Anthropic call). ACTION: add credits + set AUTO-RELOAD + a spend/low-balance ALERT at console.anthropic.com (pairs with the token-lifecycle system — no money/credit source may silently hit zero). Optional cosmetic: frontend surface billing/credit errors distinctly instead of a blank 'Something went wrong.'. src: 2026-06-28 blank-answer diagnosis. open [LC]
- TOKEN LIFECYCLE MANAGEMENT SYSTEM — LAUNCH-CRITICAL reliability. Shopify's 1h-access / 90d-rotating-refresh auto-renew (getValidShopifyToken) is the PROVEN pattern; extend proactive-renew-before-expiry + LOUD detection + clean reconnect prompt to Meta + Google; ensure NO refresh token (incl. Shopify's 90d) ever silently hits its cliff. Near-term watch: Meta cote@ token REFRESHED 2026-07-01 → new cliff ~2026-08-30 (via the admin valve below); Shopify refresh tokens ~2026-09-26. src: 2026-06-28 token prep. open [LC]
- META CALLBACK RE-AUTH BUG ✅ FIXED 2026-07-02 (LORAMER_META_CALLBACK_ONCONFLICT_V1) — src/app/api/meta/callback/route.ts:100 upserted meta_tokens with NO onConflict:'user_email' AND didn't check the returned error. meta_tokens PK=id + UNIQUE(user_email) → the upsert INSERTed → unique-violation → SILENTLY SWALLOWED → Meta RE-AUTH was a no-op for any EXISTING token (only the first-ever connect worked; the success redirect fired anyway). Broke the cote@ refresh 2026-07-01 (routed around via the admin write route). FIX SHIPPED = added { onConflict:'user_email' } + a CHECKED error (mirror the Google upsert at src/lib/auth.ts:80); on error → console.error + redirect ?meta_error=token_store_failed (no false-success). Gate-A: raw-PostgREST repro proved the buggy re-auth 409s on meta_tokens_user_email_key while the fixed path UPDATES the same row in place (id stable, 1 row). Freeze lifted for this file (Meta App Review APPROVED 07-02). Self-serve reconnect REPAIRED. src: 2026-07-01 diagnosis → 2026-07-02 fix. done
- META CALLBACK ACCOUNT-LIST-IN-URL [robustness, fast-follow] — the callback success redirect embeds the FULL Meta account list (100+ accounts) as a URL query param (/clients?meta_accounts=<huge JSON>, callback/route.ts:107-109). URL-length/fragility risk for large agencies (browser/proxy query caps). Move the handoff to a session/DB store instead of the URL. src: 2026-07-01. open [NP]
- META ADMIN REFRESH VALVE — /api/admin/meta-token-write (LORAMER_META_TOKEN_WRITE_V1, deployed 2906cec, LIVE): CRON_SECRET POST that mints (short→long via fb_exchange_token) + writes meta_tokens with the CORRECT onConflict:'user_email' upsert + a fb_user_id==10242550452717848 identity guard. Reusable for any proactive Meta refresh until the callback bug (above) is fixed; hand-run via out-of-band Graph API Explorer token. src: 2026-07-01. reference [LC]
- FULL CONVERSION/ACTION TAXONOMY — capture ALL action types + per-type values/revenue/cost-per-action/ROAS, NOT the ~5 extracted today (Meta actions[]/action_values[]; Google all_conversions; the e-commerce purchase+value tail). ★ NOW THE ACTIVE WORKSTREAM (2026-07-03, AHEAD OF P2-C) = CONVERSION-ACTION CAPTURE FLOOR — see LORAMER_DECISIONS "CONVERSION-ACTION CAPTURE FLOOR" + the MULTI-SOURCE PROVENANCE / THE MOAT law. Known gaps: Google category name-map ✅ CLOSED 2026-07-05 (LORAMER_GOOGLE_CONV_ACTION_CATEGORY_NAME_V1, fae78f8 — ordinal→name decode, forward + 120-row backfill; can now tell purchase/lead from category — see the TOP VERIFY item at the top of the ACTIVE WORKSTREAM section for the native-emission verify); STILL OPEN — Google conv-action forward-only since 06-29 (history = T2.3 quota-gated); Meta per-action drain back-covering; Google advertising_channel_type/bidding_strategy_type not persisted; Meta ~5-alias multi-count (canonicalize before ANY Meta value/ROAS card). src: inventory §1A, 2026-07-03 (name-map 2026-07-05). ACTIVE [LC]
- MULTI-SOURCE ROAS CARD (-next) — ✅ BUILT 2026-07-09 (LORAMER_NEXT_ROAS_CARD_V1): additive CardKind 'roas' (mirrors 'money') + user checkbox-group (roasBases[] persisted in the dashboard_layouts `view` JSONB; default all-checked); THREE basis-LABELED sources (A 'Meta purchase ROAS (window)' = Σvalue ÷ Σ(value/daily ROAS), spend-weighted, matches Meta Ads Manager, Gate-A proven Shelley Kyle 3990.17÷1104.15=3.6138≈3.61 · B 'Value ÷ total Meta spend' ≈1.46 · C 'Blended store ÷ Meta spend' ≈4.09, FALSE-ZERO-GUARDED on hasDataEver → no-store client renders 4.09 ABSENT not $0). STANDALONE read (src/lib/next/roas-bases.ts + /api/next/card-roas → queryRoasBases): RAW per-day omni_purchase reconstruction (only purchase_roas carrier; sibling aliases are identical-value dedup views, never summed; null/0-ROAS day SKIPPED not zeroed); queryBreakdown + dormant metaRoas carrier UNTOUCHED (shared/live read-path). Window label = ACTUAL captured omni_purchase span (action_type back-drain lag). tsc + npm run build GREEN. src: DECISIONS "MULTI-SOURCE ROAS CARD", 2026-07-09. open(Gate B render verify) [LC]
- VIDEO = ASSET **AND** METRIC, ALL-MEANS-ALL — full video ASSET layer (the creative) + full video METRIC family (plays/ThruPlay/p25-100/avg-time/30s/cost-per-thruplay), every grain, every platform. src: inventory §1A/§1D. open [LC]
- ASSET-COMBINATION CONVERSION ATTRIBUTION (core capability) — Lora names which creative COMBINATION (image/video + body + headline + CTA) drove which conversions BY TYPE, to the nickel, across Meta multi-asset + Google PMax incl. YouTube. Meta CEILING (proven): per-asset served, per-combination NOT served via Insights → must MODEL (tabled per Russ). Google PMax asset ceiling TBD at its inventory. src: inventory §1E, 2026-06-28. open [LC]
- PLATFORM ONBOARDING PLAYBOOK — the rote/repeatable template distilled from the five connected platforms (auth+token model, metric surface, dimension/breakdown + combination rules, entity grains, asset/content layer, asset-perf ceiling, retention floor, rate model, reconcile posture, storage mapping) → future platforms (Klaviyo/TikTok/Triple Whale/LinkedIn/client one-offs, all POST-7/14 + post-Q4 OK) become fill-in-the-blanks + a client-connect-one-off path. Finalize AFTER Google is mapped (most-different platform). src: 2026-06-28. open [NP]
- PLATFORM-WIDE PLAYBOOK — the whole-product operating playbook Russ has raised repeatedly; named + pending (distinct from the onboarding playbook above). src: 2026-06-28. open [NP]
- 2027 WRITE DESTINATION (the "why" behind completeness) — Lora generates near-optimal ads (1–5% differentiation) + WRITES them to Meta/Google; the entire data foundation exists to enable this. Read-only is the launch posture; write is the long-term destination (gated on the 95% accuracy stack + incremental write scopes). src: ROADMAP 2027-vision, 2026-06-28. open [NP]
- cron-runs 'drain' mode observability — CronMode is typed forward|catchup; the drain cron uses detectTrigger but SKIPPED startCronRuns/finishCronRun to avoid editing the shared lib + an enum migration. Widen CronMode to include 'drain' for sentinel observability. src: LORAMER_ONBOARD_DRAIN_V1. deferred [NP]
- Clean inert __drain_* lock rows in sync_state — dry-run + local laps left harmless NULLED __drain_ga/__drain_meta/__drain_google claim rows. One-time: `delete from sync_state where platform like '__drain_%';` (ONLY the lock rows — NOT real cursors/markers). src: LORAMER_ONBOARD_DRAIN_V1. deferred [NP]
- GA4 429 CLASSIFIER hardening — GA4 quota error "Exhausted … tokens per hour" may not match withGoogleRetry's transient regex (it tests e.message); fails LOUD now (drain ok:false, acceptable) but unclassified + no pause. GA is at floor so not currently live. Minor. src: 2026-06-28 readiness scan. deferred [NP]
- QUOTA-GUARD fold-ins (minimal-scope, deferred): (1) the account-step's 3rd retry helper backfill/retry.ts withGoogleRetry is NOT quota-typed (geo/campaign/adgroup via gaqlWithRetry already trip the pause, so the drain still pauses correctly); (2) the forward path (google-retry.ts withGaqlRetry) throws GoogleQuotaError but does NOT write the global marker — the drain self-heals into the pause on its next google lap. Fold in only to pause a beat sooner. src: LORAMER_GOOGLE_QUOTA_GUARD_V1. deferred [NP]
- AUTO-BACKFILL ON CONNECT (customer-facing): ✅ DONE 2026-06-25 (LORAMER_ONBOARD_DRAIN_V1) — marker (migration 019, column default, no connect-route touch) + drain cron /api/cron/drain (writer-registry deepest-first, PER-STEP-INDEPENDENT, self-heal floor-marking, NEVER-mark-on-error, claim-lease anti-double-fire, N-map cap, staggered every 6h). Closed 3 backoff gaps (google account + google_dimensional + meta account) + per-adapter granularMonths floor clamp (meta=36). Cohort gap-fill (GA-shallow Ennis/Veterinary/Escential/Glass Plus/Shelley; Meta placement Influential/Thought Streams) now drains automatically. Remaining writers (ad_group/ad, meta campaign/adset, breadth dims) plug in as one registry entry each. src: DATA_COMPLETENESS, ESSENCE bedrock(1), ROADMAP Data-Completeness-Onboarding. resolved [LC]
- GA page-level capture (pagePath/landingPage × views/sessions/engagement/conversions/day) → metrics_daily + query_metrics. src: AUDIT 2026-06-18. open [LC]
- Shopify deep re-backfill to full history (read_all_orders now granted on Foam OH; Influential+Escential still 3-scope — re-auth) — corrects #6 cancelled-history for free; no false-zeros. src: AUDIT#6/#7, inventory §6, LAUNCH_PARKING. open [LC]
- Shopify dimensional re-backfill beyond 60-day wall (wall lifted on re-auth). src: inventory §6. open [LC]
- Drop Woo top-10 product cap also in the captured-READ display (woocommerce-intelligence.ts:228-230) — frozen reviewer surface, post-Meta. src: this-session 1a notes. open(frozen) [EXT]
- Per-platform revenue-basis tooltips (authoritative wording — Shopify subtotal-excl, Woo incl-shipping/tax-prorata; reflect stored extra.netBasis). src: ROADMAP §Revenue-basis-tooltips. open [LC]

═══════════════════════════════════════════════════════════════════
REDESIGN PROGRAM / -next  (docs/LORAMER_REDESIGN_SPEC.md, LORAMER_NEXT_FLIP_PROGRAM.md, CONTINUE_HERE)
-next is the PRIMARY dev target; live-app freeze RETIRED (Meta approved 2026-07-02, blast-radius postures now govern). Desktop=mobile parity standing.
═══════════════════════════════════════════════════════════════════
- Per-platform DRILL PAGES on -next (/dashboard-next/{google-ads,meta-ads,shopify,analytics} — routes don't exist) → capability parity w/ live. src: CONTINUE_HERE NEXT STEP(06-19). open [LC]
- Agency profile & settings (top-right agency-avatar destination). src: ROADMAP Redesign LC, CONTINUE_HERE queue. open [LC]
- Per-client profile card (Mer front: identity/context/thresholds/rules; logo upload + monogram). src: ROADMAP Redesign LC. open [LC]
- Team members & permissions (RBAC): role tiers, per-client scoping, capability gates, 2 admin scopes. (RBAC foundation slice 1A shipped — migration 018, wired nowhere.) src: ROADMAP Redesign LC, CONTINUE_HERE RBAC. partial [LC]
- SURFACING TRACK (the launch-binding gap per the readiness audit) — #1 CARD ENGINE ✅ BUILT 2026-06-29 (LORAMER_NEXT_CARD_ENGINE_V1; dark in -next, Overview-wired, PAGE-AGNOSTIC = pageKey + SavedView so portfolio/client reuse it with NO rebuild; react-grid-layout drag+resize desktop / reorder-only mobile [locked C]; named saved views [locked A]; side-panel config [locked B]; src/components/redesign/cards/ + /api/next/card-breakdown + /api/next/layouts; Gate A tsc-green + default Overview view renders REAL data [stat via client-metrics · timeseries via client-timeseries · age breakdown via queryBreakdown, 849 rows verified]). dashboard_layouts MIGRATION ✅ APPLIED 2026-06-29 (migration 022; UNIQUE NULLS NOT DISTINCT; save/load/set-default round-trip PROVEN at the DB layer) — named-view persistence LIVE. #2 QUERY-LAYER/ALLOWLIST ✅ ADDITIVE FAMILIES DONE 2026-07-02 (LORAMER_QUERY_ALLOWLIST_BREADTH_V1): multi-platform (platform,breakdown_type) keying + allowlist adds {device, device_platform, hour, Meta geo_country/geo_region, action_type + conversion_action ranked by conversions} + `variant` via query_metrics level + publisher_platform→placement repoint; Gate-A byte-identical on existing types (age, shopify geo_country) + per-family read-back proven. REMAINING: impression_share + video → P1b ✅ DONE 2026-07-02 (LORAMER_QUERY_NONADDITIVE_V1 — per-entity projection in queryBreakdown gated to {impression_share, video}: IS 7 ratios POINT-IN-TIME most-recent-day per campaign [never aggregated]; video 8 counts summed + 2 rates singleVal [null across multi-day], entityLevel default 'campaign' [no cross-level double-count]; Gate-A byte-identical pre/post on age/device/hour via git-stash, IS/video read-back on real rows); money → P1c ✅ DONE 2026-07-02 (LORAMER_QUERY_MONEY_V1 — dedicated query_money tool [account-grain gross→net waterfall, reuses aggregateMoney → reconciles with /api/next/money by construction; platform-scoped shopify/woo; Gate-A EXACT reconcile both clients vs DB sum]). ✅ #2 QUERY-LAYER SURFACING FULLY DONE — device/geo/hour/age/gender/action_type/conversion_action/placement + impression_share/video + variant(level) + money are all Lora-queryable. #3 = BREADTH CARDS ⇐ {#1,#2}. Meanwhile build more -next cards on the 6 query-exposed families (age/gender/geo_country/geo_region/search_term/keyword). #1b RESHAPE ✅ BUILT 2026-06-29 (LORAMER_NEXT_CARD_ENGINE_RESHAPE_V1: global page date range [Shopify model] + per-card override + COMPARE [green/red deltas + dashed comparison overlay + dual-range legend] + full-screen + dual-corner resize se+sw desktop-only + dead-Customize killed; client-metrics/timeseries/card-breakdown extended for explicit current+compare windows; tsc-green). DEFERRED fast-follow: good/bad-direction delta polarity (color by metric meaning — cost/CPA up=bad) + -next breakdown BAR TOOLTIP shows the internal key 'v' — replace with the real metric label (e.g. "Clicks: 17" / "Spend: $45"), matching the table-header label (CardViz BarChart <Tooltip>). #4 PREBUILT CARD LIBRARY (queued surfacing) — the two-step Add: Customize → [Prebuilt | Build-your-own] → platform → categorized sidebar (each platform's OWN report categories per inventory §6b). Shell buildable NOW; each prebuilt entry lights up only as its data clears query-exposure (#2) + capture fill (Tier 4). src: card-engine flights 2026-06-29h/j + prebuilt-catalog audit, redesign §1/P18. #1 + #1b done [LC]
- P2 -NEXT PARITY BUILD (LORAMER_NEXT_PARITY_V1) — the two cutover blockers per the cutover audit (LORAMER_NEXT_TO_LIVE_AUDIT_V1): the Ask-Lora chat MOAT + per-platform drill. Build order A→B→C→D. P2-A ✅ DONE 2026-07-02 — redesign-native Ask-Lora ChatPanel (src/components/redesign/ChatLauncher.tsx + chat.module.css; thin client over the UNCHANGED /api/chat [owner-scoped, ZERO backend]; desktop right-docked slide-over / mobile full-screen sheet; TopBar pill + mobile Lora tab both trigger it via a 'loramer:open-chat' event). Gate-A FUNCTIONAL proven pre-commit via a forged-but-valid NextAuth session driving the exact /api/chat call — coherent answer exercising the NEW hour tool (top hours 16/21/20 $437.68/$386.68/$362.12, matching captured rows); tsc+build green. VISUAL (mobile+desktop) = Gate-B on deploy (Russ on-device; -next is preview-gated). P2-B ✅ DEPLOYED to -next 2026-07-03 (LORAMER_NEXT_KW_ST_CARD_V1) — Gate-B on-device PENDING. Two-file -next-only build: (1) CardViz.tsx BreakdownBody — a truncation/advisory note no longer HIDES rows (kw/st ALWAYS carry the "top N of M subset" note; now rendered as a muted caption BENEATH the top-N rows; a note still shows in-place only when there are 0 rows); (2) card-types.ts defaultOverviewView — two prebuilt Google breakdown cards d-kw (keyword) + d-st (search_term), viz='table', rankBy='spend', topN=8, new bottom row y=13 (nothing above moves). ZERO change to metrics-query/queryBreakdown/card-breakdown route/BREAKDOWN_CATALOG (kw/st were already coming:false query-exposed). Gate-A GREEN: tsc+build exit 0; read-back Bath Fitter | O'Gorman Bros (60e6dd99) keyword 207 distinct / search_term 4,662 distinct → both >8 so the note fires, rows return (top spend $9,230.60 / $7,853.19). Gate-B = Russ on-device -next: both cards render top-8 rows with the "subset" caption beneath (not in place of the rows). NEXT = P2-C: /api/next/entities BACKEND ✅ BUILT 2026-07-10 (LORAMER_NEXT_ENTITIES_V1, 55263ef — captured entity-hierarchy read via NEW queryEntities: google+meta, campaign→ad_group/ad_set→ad drilled by parent_entity_id, membership-aware resolveAccess, captured-only [NO live platform call]; Gate-A GREEN tsc + npm run build, real-data proof Veterinary both platforms [google 4 campaigns $2494.69 · drill→ad_group $1299.38 · meta campaign→ad_set→ad $2633.36], totals=Σ rows, owner-ok/non-member-404; parent linkage 100% google+meta verified 2026-07-10). SPINE INCR 2 ✅ BUILT 2026-07-10 (LORAMER_NEXT_PLATFORM_PAGE_V1, c74959c): NEW route /dashboard-next/[platform] (slug→platform map; static routes win) + <PlatformPage> (date-range → period-bus) + <DrillView> (drill state {depth,parents} + Breadcrumb + browser pushState/popstate + /api/next/entities fetch + platform-scoped TOTALS strip = the honest summary) + <DrillTable> (sortable cols + totals row + row-drill + column picker + per-row Ask-Lora ✦ → dispatches 'loramer:open-chat' with rowContext; ChatLauncher got ONE additive rowContext passthrough). GOOGLE end-to-end; tsc + npm run build GREEN; Gate-A render/logic/drill-state/ask-payload ALL PASS; /api/chat + legacy + preview gate UNTOUCHED. Gate-B on-device PENDING (Google: campaigns render · drill campaign→ad_group→ad · breadcrumb + browser-back · sort/totals · ✦-opens-chat-with-row-context · sm+desktop). REMAINING spine: Meta ✅ VERIFIED 2026-07-10 (LORAMER_NEXT_PLATFORM_SPINE_V3, d68b781 — campaign→ad_set→ad reconciles to the penny, SAME components, no rebuild) · ga/shopify SHELLS ✅ BUILT 2026-07-10 (styled coming-soon, rail RESOLVES, NOT 404; real GA tables = N4 [capture-gated] + Shopify ecom cards = N3 remain their OWN flights) · timeseries GRANULARITY toggle (N1) · PLATFORM-SCOPED summary cards (CardEngine summary blocked — client-metrics/timeseries are portfolio-combined; needs platform-scoped endpoints) · P2-D deeper ad-drill detail = PENDING. RBAC lands BEHIND P2 (cutover parity first). Supersedes the older "Per-platform DRILL PAGES on -next" line (folded into P2-C/D). NOTE (2026-07-02): between P2-A.1 and P2-B the shared read-path was hardened — breakdown index (LORAMER_BREAKDOWN_INDEX_V1, 71c2ca5, migration 024 applied to prod), entity-level scoping (LORAMER_BREAKDOWN_LEVEL_SCOPE_V1, f110feb), Google hour-0 catch-all note (LORAMER_GOOGLE_HOUR0_NOTE_V1, 56f472e). src: cutover audit + P2 design 2026-07-02; P2-C backend 2026-07-10. P2-B deployed · P2-C backend + spine incr 2 (PlatformPage/DrillView/DrillTable, Google) BUILT; Gate-B + Meta-verify + ga/shopify + granularity + platform-scoped-summary + P2-D pending [LC]
— PLATFORM-PAGE GATE-B PUNCH-LIST (LORAMER_NEXT_PLATFORM_PAGE_V1, 2026-07-10): Gate-B CORE PASSED (drill campaign→ad_group→ad · breadcrumb · browser-back · column sort · totals row · per-row Ask-Lora ✦ — the spine is SOUND). Three DEFERRED-POLISH findings, NOT spine-blocking:
- P-PL#1 DATE RANGE — the platform page offers only Last 7/14/30/90; needs the FULL preset set + CUSTOM picker to match the range control used elsewhere (or adopt Google's default set). SHARED date-control work → FOLD into the timeseries-GRANULARITY-TOGGLE flight (same date machinery). src: Gate-B 2026-07-10. open(deferred-polish) [LC]
- P-PL#2 COLUMN PICKER — ✅ FIX BUILT 2026-07-10 (LORAMER_NEXT_PLATFORM_SPINE_V3, d68b781): the "Columns ▾" menu is now PORTALED to <body> (position:fixed at the button rect + max-height 60vh/overflow-y scroll + full-viewport backdrop) → escapes .tableCard's overflow:hidden clip; all options reachable. Gate-A render/CSS proof passed. Gate-B on-device (tap-reachability sm+desktop) PENDING. src: fix 2026-07-10. open(Gate B) [LC]
- P-PL#3 MOBILE LORA CHAT — the Ask-Lora input box is far too small on mobile; must match Claude/Anthropic mobile chat: auto-scroll to bottom on new message, input GROWS with content, finger/scroll-reactive, full-height sheet. SHARED ChatLauncher work → its OWN flight, inherited by EVERY -next surface. src: Gate-B 2026-07-10. open(deferred-polish) [LC]
- STORE PLATFORM PAGE (Shopify/Woo · N3 · path B) — the -next store surface. FLIGHT 1 ✅ BUILT 2026-07-11 (LORAMER_NEXT_STORE_READS_V1, 5239c98): store-scoped READ endpoints, connection/data-aware Shopify|Woo. THREE reads, all resolveAccess-gated + force-dynamic + captured-only — /api/next/store-stats (net revenue · orders · AOV via queryMetrics platform=[store] level=account; DEDICATED store route, consistent with /api/next/money not a portfolio-combined param) · /api/next/store-timeseries (daily {date,revenue,orders} via NEW queryStoreTimeseries; no-sale days ABSENT by the writer's false-zero law, a gap in the line not a $0) · /api/next/entities WIDENED to shopify|woo · product|variant (top products by revenue+units+name; google|meta BYTE-IDENTICAL — queryEntities untouched, only the route allowlist grew). Detection: src/lib/next/store-detect.ts resolveStorePlatform — explicit request if it has data else most-recent captured account date wins; NEITHER connected → chosen:null → honest empty/connect state (hasDataEver law, NEVER a false $0). STEP-0 ORDERS-SEMANTICS FINDING (verify-first, banked): account.conversions = ORDER COUNT (shopify-metrics-row.ts:57 data.totalOrders / woocommerce-metrics-row.ts:29 same); product/variant.conversions = UNITS — so the Orders stat reads account.conversions and top-products reads product.conversions(units); no stat built on an unconfirmed field. Gate-A GREEN: tsc + npm run build exit 0; the reads' EXACT aggregation proven on real June data on BOTH platforms — Foam OH (shopify: rev $1054.00/orders 10/AOV $105.40 · 30 ts days · top 'FoamOh Super Castle Play Sofa' $379/2u) + Shelley Kyle (woo: rev $2885.45/orders 39/AOV $73.99 · 30 ts days · top 'De Ma Mere Crystal Vanity Set' $266.50/2u); connection-aware detection Foam OH→shopify (woo_latest null) / Shelley→woo (shopify_latest null); Σproducts==account revenue both. Blast radius = 5 store-read files only (2 modified: entities allowlist + metrics-query pure-add; 3 new); no legacy, no shared component, no existing route behavior changed. DEPLOYED to -next 2026-07-11. FLIGHT 2 ✅ BUILT 2026-07-11 (LORAMER_NEXT_STORE_PAGE_V1, d096067): the store PAGE UI — REUSES CardEngine (pageKey store:{clientId}) with a NEW storeDefaultView; connection-aware (resolveStorePlatform → CardEngine when a store has data, else an honest StoreEmpty connect-state, NEVER a false $0). Cards: Net revenue · Orders · AOV (store-stats) · Revenue&orders timeseries (store-timeseries, Revenue|Orders toggle) · Top products (entities product) · Money breakdown (the existing 'money' kind FOLDED IN from the now-RETIRED money-only /store) · Customer mix (honest coming-soon). Store cards switch endpoints via CardConfig.source==='store' in useCardData/CardViz — Overview BYTE-IDENTICAL (defaultOverviewView carries no source; every store branch gated). SINGLE connection-aware 'Store' rail entry REPLACES the 'shopify' interstitial (RailContent) + a Store tab added to MobileNav; NO 'woocommerce' entry (one Store serves both per detection). Gate-A GREEN: tsc + npm run build exit 0; every card populates on real data (LAST_30_DAYS 06-11→07-10) on BOTH platforms — Foam OH shopify (rev $1025 · orders 6 · AOV $170.83 · 29 ts days · top 'FoamOh Original Play Castle Sofa' $762/3u) + Shelley Kyle woo (rev $2405.55 · orders 30 · AOV $80.19 · 29 ts days · top 'De Ma Mere Crystal Vanity Set' $266.50/2u); empty-state proven on real neither-connected clients (Ennis Exterminating / Veterinary mastermind → chosen null → StoreEmpty). Blast radius = 6 -next files (cards + nav + store page); no legacy, no shared-component behavior change, no other route touched. Gate-B on-device PENDING (grid draw · card add/remove/rearrange · tap-reachability · sm+desktop MOBILE-PARITY). REMAINING: CUSTOMER-MIX card (still coming-soon — GATED on the 0-PII customer engine, unbuilt) · timeseries GRANULARITY toggle (P-PL#1) · date-range presets + CUSTOM (P-PL#1). src: N3 store-page scope 2026-07-10 + store-reads + store-page builds 2026-07-11. FLIGHT 1+2 done; customer-mix + granularity/date-range OPEN [LC]
— STORE PAGE GATE-B FINDINGS (LORAMER_NEXT_STORE_PAGE_V1, 2026-07-11): grid RENDERS, all cards POPULATE on Shopify (Foam OH) + Woo (Shelley). S-PL#1+#2+#3+#4 ✅ DONE; OPEN: S-PL#5 (compare legend/axis) + S-PL#6 (store-catalog BREADTH) + the STANDING UX BAR (with today's label-truncation defect). All [LC] unless noted.
- S-PL#1 CLIENT-SWITCH CRASH — ✅ DONE 2026-07-11 (LORAMER_NEXT_STORE_SWITCH_FIX_V1, aa357c7): the store page's "Application error: client-side exception" on client switch (FoamOh↔Shelley↔Vet) is FIXED. ROOT CAUSE = an un-keyed CardEngine REUSED across the TopBar SOFT switch (router.push(?clientId=…) keeps the pathname) → the reused instance retained the PRIOR client's cards + baked storePlatform + RGL/recharts subtree through the async [pageKey,clientId] re-apply window → a stale-transition reconciliation crash (candidates #1 react-grid-layout / #2 recharts reused subtree). FIX = ONE line: key={`store-${resolved.id}`} on the store CardEngine (store/page.tsx) → every switch MOUNTS a fresh engine seeded from storeDefaultView(chosen); no reused instance, no stale platform, no reused RGL/recharts subtree. NO band-aid try/catch or per-card guards. Candidate #3 (shared card id → different kind → hook crash) RULED OUT: storeDefaultView ids/kinds are fixed+identical across clients, Add-card mints unique time-based ids, and CardViz is a hook-LESS dispatcher to DISTINCT sub-components (React remounts on component-type change — NOT a #300 hook-count violation) → not real, nothing latent to bank. Gate-A GREEN: tsc + npm run build exit 0; key present w/ the resolved id; blast radius = store/page.tsx ONLY (Overview + CardEngine/CardGrid/CardViz/card-types/useCardData byte-identical). Gate-B on-device PENDING (switch FoamOh↔Shelley↔Vet with NO crash; each remounts clean). src: S-PL#1 investigation + fix 2026-07-11. done [LC]
- S-PL#2 REVENUE GRAPH IGNORES COMPARISON — ✅ DONE 2026-07-11 (LORAMER_NEXT_STORE_COMPARE_V1, e8128b2): the Revenue&Orders timeseries card now OBEYS the page compare — a SECOND read of the SAME /api/next/store-timeseries for the compare window (no route change), overlaid as current (solid #2563eb) + prior (dashed #94a3b8) with a dual-range <Legend>, the SAME treatment as CompareLine. Aligned by DAY-OFFSET from each window's start (day 1 vs day 1) — offset-based not array-index because store days are SPARSE (no-sale days absent by the false-zero law); absent days stay gaps (null), NEVER a fabricated $0. Revenue|Orders toggle preserved in both branches; compare-OFF byte-identical to before. 1 file (CardViz.tsx StoreTimeseriesBody + its dispatcher line); other card bodies + the route UNTOUCHED. Gate-A GREEN: tsc + npm run build exit 0; both windows populated on real data — Foam OH shopify (current 29d $1025.00 · compare 30d $4119.00 · 29 shared-offset days) + Shelley woo (current 29d $2405.55 · compare 29d $2418.24 · 28 shared) — overlay draws both lines, gaps stay null. Gate-B on-device PENDING (both lines + legend + toggle render sm+desktop). src: S-PL#2 fix 2026-07-11. done [LC]
- S-PL#3+#4 STORE-SCOPED CARD CATALOG — ✅ DONE 2026-07-11 (LORAMER_NEXT_STORE_CATALOG_V1, eb83806; Gate-B PASS 2026-07-11): source-AWARE add-card picker (CardConfigPanel takes source+storePlatform, threaded store page → CardEngine → panel). On a store page: Stat metric list = STORE_STAT_METRICS (Net revenue / Orders / AOV, NOT spend/roas/clicks); Breakdown list = ONLY store families filtered to the active storePlatform; engine new-card default inherits source:'store'+storePlatform so "Add card" mints a store card, not a portfolio spend card. Store families added to BREAKDOWN_CATALOG keyed platform+breakdown_type — product + variant (query-exposed via /api/next/entities; variant READ-PROBED clean on real Foam OH rows: 7 named variants, revenue>0, no false zeros), customer_mix coming:true (0-PII engine unbuilt → picker-disabled). Overview add-card BYTE-IDENTICAL (STORE_FAMILIES partition keeps store families off the ad picker + ad families off the store picker). 6 -next files; tsc+build green; Gate-B on-device PASS 2026-07-11. src: S-PL#3+#4 build 2026-07-11. done [LC]
- S-PL#6 STORE-CATALOG BREADTH (reinforced 2026-07-11, Gate-B) — the source-aware catalog WORKS but is THIN: it exposes only product/variant/customer_mix. Expand the store family catalog BROADLY — every captured Shopify/store grain we ALREADY hold should become an addable PRESET store card, e.g. orders, refunds, discounts, taxes, shipping, geo (country/region — geo_country/geo_region already captured), sales channel, fulfillment status, repeat-vs-new (gated on the 0-PII customer engine), etc. Much of this is ALREADY captured (account extra.money holds discounts/taxes/shipping/refunds per LORAMER_ECOM/SHOPIFY_MONEY_SURFACE_V1; geo families captured) — the build is (a) a broad store add-card registry, (b) the reads to back each family (money-surface breakdown reads + entities/geo), (c) preset cards. Same platform+breakdown_type keying as S-PL#3+#4. src: Gate-B 2026-07-11 (catalog live but thin). open [LC]
- S-PL#5 STORE TIMESERIES LEGEND/AXIS — the compare legend shows the SAME window label for BOTH current and prior (both read e.g. "Apr 12–Jul 10") → must label them DISTINCTLY (current vs prior window, like Shopify's 2026 vs 2025). ALSO: the compare X-axis uses day-offset numbers (7/18/30) for sparse-day alignment — reads worse than real dates; evaluate a REAL-DATE axis that STILL handles sparse (no-sale-absent) store days. src: Gate-B 2026-07-11. open [LC]
- STANDING UX BAR (cross-surface quality gate, NOT a one-off) — every -next UI flight must reach SHOPIFY-ADMIN-CALIBER mobile UX: smooth charts, REAL-DATE axes, clean + DISTINCT legends, Claude-caliber chat (per P-PL#3), finger-reactive. MEASURE every -next UI flight against this bar; a flight that regresses it is not done. CONCRETE DEFECTS logged against this bar (fix as the relevant card/flight comes up): (1) 2026-07-11 — store BREAKDOWN card Y-axis LABELS TRUNCATED ("FoamOh Original Pl", "FoamOh Super Castl"): the CardViz BarChart YAxis width=96 hard-clips category names → reads bush-league vs Triple Whale. Needs TW-caliber label handling — full names via wrap or ellipsis-WITH-tooltip + a responsive label column that widens for long names; applies to ALL breakdown bar cards (store products/variants most visible). src: store Gate-B 2026-07-11. standing [NP]
- NATIVE AUTH — DECOUPLED IDENTITY (LAUNCH-CRITICAL, do-before-2026-07-14). SLICE 1 ✅ SHIPPED 2026-07-12 (LORAMER_NATIVE_AUTH_V1, migration 029 auth_credentials APPLIED): email/password provider appended to auth.ts (Google/reviewer/shopify + callbacks untouched) + POST /api/auth/signup (credential mint only, bcryptjs cost 10, dup→409) + /login + /signup UI + two-door "sign up with email" link; native session byte-identical downstream (session.user.email), feeds the SAME spine (account_type via cookie → /welcome → provisioning). SLICE 2(a) ✅ SHIPPED 2026-07-12 (LORAMER_NATIVE_AUTH_ALLOWLIST_V1, migration 030): invite-only gate LIVE both doors — signup_allowlist (seed 3 cohort + backfill owners/members/clients) + shared isAllowed predicate + auth.ts google signIn callback + /api/auth/signup 403 not_invited + org/invite auto-allowlist + /api/interest (Mailchimp audience 5bf7067007, single opt-in, writes ONLY Mailchimp) + ungated /request-access screen; MAILCHIMP_API_KEY set Prod+Preview. PENDING: BULK-SEED remaining cohort from Russ's spreadsheet (scoped INSERT source='seed'). REMAINING SLICE 2: (b) email VERIFICATION + password RESET (STILL deferred — need a transactional sender, Resend: 1 dep + RESEND_API_KEY + verified domain; reset impossible without it), (c) Google-Ads DATA decoupling (ga/connect-cloned post-login connector). — ORIGINAL slice-2(a) recon retained: allowlist + Mailchimp interest-capture + invite-only screen [no Mailchimp integration exists in THIS repo/Vercel project — the landing repo has it; needs a fresh MAILCHIMP_API_KEY+audience id here or copied from loramer-landing], (b) email VERIFICATION + password RESET — BOTH require a transactional email sender (Resend recommended: 1 dep + RESEND_API_KEY + verified domain); reset is IMPOSSIBLE without it, verification deferrable for the invite-only cohort, (c) Google-Ads DATA decoupling (ga/connect-cloned post-login connector so a native user connects Google Ads independent of login). — original recon retained: add LoraMer-native email/password signup BESIDE the untouched Google login, and decouple Google-Ads DATA capture from the Google LOGIN. RECON PROVEN 2026-07-11 (read-only): (a) KEEP GOOGLE LOGIN 100% UNCHANGED — zero change to the GoogleProvider block / adwords scope / consent screen; native signup = a PURE providers[] APPEND (auth.ts already runs 2 CredentialsProviders — reviewer-token + shopify-install — alongside GoogleProvider, the live proof coexistence is isolated); JWT sessions are already the NextAuth default (no adapter) = exactly what a CredentialsProvider needs. (b) IDENTITY STAYS EMAIL-KEYED / auth-method-AGNOSTIC — a native user's session.user.email has the identical shape and flows through resolveAccess / listAccessibleClients / clients.user_email identically; reviewer + shopify-install are the live non-Google precedent (no refreshToken, no google_tokens row, app works). Google-identity hard-deps are scoped ONLY to Google-ADS data (session.refreshToken, google_tokens row, adwords scope) — core app + org/RBAC need zero Google. (c) PIECES to add (not built): password_hash storage (a column on user_profiles OR a dedicated auth_credentials table — cleaner) · bcryptjs (pure-JS, Vercel-safe) · a 3rd CredentialsProvider id='password' (mirrors the existing 2) · a /api/auth/signup route · email verification (OPTIONAL for launch, add later) · password reset (defer-able). STAY IN NextAuth — Supabase Auth is NOT used for login; do NOT fork the identity model. (d) GOOGLE-ADS DATA DECOUPLING — Google Ads is the ONLY login-coupled platform: the adwords refresh_token is captured ONLY at login (sole google_tokens WRITER = auth.ts jwt callback). Decouple via a POST-LOGIN "Connect Google Ads" connector that CLONES /api/ga/connect (getServerSession → adwords OAuth consent offline+consent → upsert google_tokens keyed to the logged-in session.user.email, any auth method). Meta/Shopify/Woo/GA are ALREADY post-login connectors (per-user OAuth, independent of login). (e) TEST-FIXTURE NOTE — ACCOUNT-CONVERSION (a Google-OAuth user CHOOSING to hold a LoraMer-native credential; Google-identity → native account) is a capability this flight MUST cover. No conversion flow exists today. When native auth ships, DELIBERATELY re-create a Google-identity fixture to test conversion — do NOT preserve stale accounts for this; Renee (reneedriscoll61509@gmail.com, DELETED 2026-07-11, LORAMER_CLEANUP_DELETE_RENEE_JASON_V1) was the original prompt for this note. Pairs with RBAC Path B (org entity) — both are the identity/access launch arc. src: native-auth + data-decoupling recon 2026-07-11. open [LC]
- RBAC PATH B — ORG/TEAM arc (member-access launch arc; pairs with NATIVE AUTH above). SPINE ✅ APPLIED 2026-07-11 (LORAMER_RBAC_ORG_SPINE_V1, migration 026): organizations + org_members + org_client_grants + clients.org_id (NULLABLE, indexed); backfilled 1 org/owner — Cote Media (agency, owner cotebrandmarketing@) + 5 solo fixture orgs; org_id 0-null, Cote Media=20, user_email untouched. ACCESS-LAYER ✅ SHIPPED 2026-07-11 (LORAMER_RBAC_ACCESS_ORG_V1, 37ccec3): org-aware resolveAccess (owner → org-member-via-grant → legacy client_members → fail-closed) + listAccessibleClients; owner-only gates (chat · intelligence [ownerEmail-threaded + owner Google token] · claude-tools query_metrics · -next overview/store pages) SWAPPED onto it → member Lora unblocked; owner path byte-identical; Gate-A 11/11 cross-org isolation proven. INVITE/TEAM ✅ SHIPPED 2026-07-11 (LORAMER_RBAC_INVITE_V1, 2edc0fa): /api/org/{invite,revoke,team} + /dashboard-next/team + TeamPanel (solo simple / agency per-client checklist); caller scoped to own org, cross-org grant BLOCKED (proven), owner non-revocable, idempotent; Gate-A 9/9 sim. MEMBER ROUTING ✅ SHIPPED 2026-07-11 (LORAMER_NEXT_CUTOVER_V1, 9bdf4e7): -next is the DEFAULT post-login surface for all authenticated users; /dashboard redirects real users to /dashboard-next; welcome-gate MEMBERSHIP-AWARE (a member/owner-with-access is NOT a new user → fixes RBAC bug B); LEGACY review/demo cohort (shopify-reviewer/demo/shopify+* fixtures) HELD on the current UI (Shopify App Store now APPROVED — the hold is no longer Shopify-review-gated; it persists only because those fixtures still expect the legacy UI); Billing menu wired; Gate-A 7/7 routing + Gate-B PASS (member cote.russell@ routes INTO the org, no onboarding). BUG A ✅ SHIPPED 2026-07-11 (LORAMER_RBAC_INVITE_GRANT_UX_V1, d3fbdeb): TeamPanel all-clients master-checkbox → explicit Specific|All segmented control, default Specific → all-clients is DELIBERATE-only, never accidental; Gate-A 5/5 payload + Gate-B PASS (cote.russell@ re-invited specific→Foam OH, switcher shows ONLY Foam OH). ORG-PROVISIONING ✅ SHIPPED 2026-07-12 (LORAMER_RBAC_ORG_PROVISION_V1): src/lib/access/ensure-org.ts → ensureOrgForOwner(email,{defaultType,name?}) resolve-or-create org (idempotent 1-org/owner, fail-LOUD so a creator NEVER inserts an org-less client, race-tolerant now → race-SAFE once organizations.owner_email is unique) + owner org_members row; WIRED into BOTH (only) client-create paths — POST /api/clients (defaultType 'agency') + shopify/callback Branch B install (defaultType 'solo', name=store, LIVE-PATH: org-fail aborts the install cleanly, never an org-less client). Every new client (incl. future + Shopify installs) now born WITH org_id; Gate-A 8/8 (all 3 flows + reuse) + tsc/build GREEN. Gate-B (a) ✅ PASS 2026-07-12 — a REAL live Add-client create ("Zz org_id test", owner cotebrandmarketing@, deployed 01b5b79) SET org_id + REUSED Cote Media's org a819bc1f… (no duplicate org spun up); bare test client verified child-free across all 10 FK tables + metrics_daily/sync_state, then DELETED (this wrap), owner back to 20. NOT-NULL LOCK: migration 027_rbac_org_id_notnull.sql (UNIQUE INDEX organizations(owner_email) + clients.org_id SET NOT NULL) ✅ APPLIED LIVE 2026-07-12 (via Supabase MCP) — preconditions re-verified live at apply (0 null org_id of 28, 0 dup owner_email); post-apply schema confirmed (clients.org_id is_nullable=NO · organizations_owner_email_unique present). Gate-B (b) CLOSED via a REAL Shopify App Store install (Cozy Foam Factory / cozy-sack.myshopify.com) that landed B-NEW on shopify/callback Branch B (org→client→shopify_installs minted in-order within 0.7s; provenance in CONTINUE_HERE resume entry). The provisioner (LORAMER_RBAC_ORG_PROVISION_V1) is now race-SAFE — the unique index guarantees a lost-race re-select finds the winner's row. org_type PERSISTENCE ✅ SHIPPED 2026-07-12 (LORAMER_ORG_TYPE_PERSIST_V1, migration 028 applied+backfilled live): the two-door (agency vs business) choice now persists via user_profiles.account_type (homepage cookie → /welcome atomic authoritative write; FORCED-CHOICE gate requires it) and drives org_type at first client-create — POST /api/clients reads account_type + maps business→'solo' (the ONLY translation, so org_type CHECK never sees 'business') + 409s on null (no silent default), replacing the hardcoded 'agency' default. Shopify B-NEW unchanged ('solo'). Backfill: 4 existing profiles set from org_type, 1 member stays null (expected). RBAC ARC END-TO-END: spine ✅ · access ✅ · invite/team ✅ · member routing ✅ · bug A ✅ · scoping Gate-B ✅ · org-provisioning ✅. src: RBAC Path B arc 2026-07-11 + provisioning 2026-07-12. arc: provisioning ✅ (Gate-B a) · not-null lock ✅ APPLIED 2026-07-12 (Gate-B b CLOSED via Cozy Foam live install) → RBAC arc COMPLETE end-to-end, no migration steps remain [LC]
- RBAC NOT-NULL LOCK ✅ DONE 2026-07-12 (LORAMER_RBAC_ORG_ID_NOTNULL_V1, migration 027 APPLIED LIVE) — clients.org_id SET NOT NULL + UNIQUE INDEX organizations(owner_email); preconditions re-verified live at apply (0 null org_id of 28, 0 dup owner_email); Gate-B (b) CLOSED via the Cozy Foam Factory live Shopify App Store install (B-NEW on shopify/callback Branch B). The FINAL RBAC-migration step — COMPLETE; RBAC arc now end-to-end done (spine · access · invite/team · member routing · bug A · provisioning · lock). src: RBAC Path B 2026-07-11 + lock 2026-07-12. done [LC]
- TEAM-MANAGEMENT V2 [LC, after the arc] — (a) EDIT-MEMBER-ACCESS: owner/admin can ADD/REMOVE specific clients on an EXISTING member (today = invite/revoke + idempotent-replace only; no real edit surface). (b) DUPLICATE-ADD UX GUARD: the backend is idempotent (upsert) but the UI gives NO "already on your team" warning — re-adding an existing email silently OVERWRITES their grant; add a warn/confirm. (c) OPEN PRODUCT QUESTION (Russ decides, shapes the v2 build): can ADMINS manage members + edit client grants, or is that OWNER-ONLY? (Meta model = admins manage people; an agency may want client-access changes owner-only.) src: RBAC invite Gate-B 2026-07-11. open [LC]
- -NEXT MANUAL ADD-CLIENT ✅ DONE 2026-07-12 (LORAMER_NEXT_ADD_CLIENT_V1) — owner-only "Add client" button + name-only modal in the -next clients surface (MultiClientOverview) → POST /api/clients (REUSED, unchanged — org_id via ensureOrgForOwner + account_type gate); member-gated (canAddClient = owns-org OR no-membership); authoritative on failure; router.refresh reveals the new card. NO migration. OPEN defense-in-depth follow-up [LC]: POST /api/clients has NO backend member-gate (PRE-EXISTING — any authed user can POST → own-email/new-org client); a shared-route owner-check is a separate small flight (not bundled). src: Russ live obs 2026-07-12. done [LC]
- LEGACY /clients IS FULLER THAN THE -NEXT CLIENTS SURFACE (CUTOVER-COMPLETENESS, gates legacy retirement 2026-07-14) — legacy /clients has client-management capabilities the -next clients surface lacks (manual add + richer management, per Russ live obs). DO a dedicated scoping pass: enumerate EXACTLY what legacy /clients does that -next does not; ALL of it must port before legacy can be retired (the cutover flip cannot drop capability). Pairs with -NEXT MANUAL ADD-CLIENT above. src: Russ live obs 2026-07-12. open [LC]
- GRANT-FIX IS NOT RETROACTIVE (banked 2026-07-11): the bug-A UI fix (LORAMER_RBAC_INVITE_GRANT_UX_V1) prevents FUTURE accidental all-clients grants; it does NOT retro-change EXISTING all_clients grants. Correct a mis-granted member by RE-INVITING via the fixed UI (Specific → the intended clients) — the route replaces grants idempotently. cote.russell@ was corrected to Foam-OH-only this session. src: bug-A fix 2026-07-11. done
- INTELLIGENCE FAIL-PARTIAL ✅ SHIPPED 2026-07-11 (LORAMER_INTELLIGENCE_HARDENING_V1, 064dd54) — /api/intelligence 6-read Promise.all→allSettled + guarded google_tokens/HEALTH_UI awaits + readOnce(1 retry/150ms) + structured per-source logs → ALWAYS returns the partial context; "platform undefined" structurally unreachable once the client is accessible. Root cause of the owner empty-context symptom = a transient Supabase ECONNRESET fail-closing the whole route (now fail-partial). Gate-A 17/17; Gate-B PASS in prod (a Google blip degraded gracefully with a banner). src: intelligence hardening + incident 2026-07-11. done [LC]
- STALE-WHILE-REVALIDATE on /api/intelligence [fast-follow] — on a HARD read failure AFTER retry, serve last-known-good from client_context.intelligence_cache with an AGE marker + background-refresh (the "never blind" layer). retry(1,150ms) proven INSUFFICIENT for some Google blips (prod banner observed). Same file; the cache table already exists. src: 2026-07-11. open [LC]
- READ-PATH RESILIENCE L4+ [post-launch] — circuit-breaker + fallback metrics (Cockatiel-class) on the read-path; overkill for the founding cohort, right for scale. src: 2026-07-11. open [NP]
- VALUE-MODEL POPUP polish [later] — flesh out the "Set this client's value model to continue" modal (Russ likes it, wants it fully built). src: 2026-07-11. open [NP]
- ALL-CLIENTS PAGE ON MOBILE — ✅ RESOLVED not-a-regression 2026-07-11: Russ located the per-client brain page live; NOT a missing surface. Do not re-investigate. src: 2026-07-11. done
- INFLUENTIAL DRONES DUPLICATE — CONFIRMED still present 2026-07-11 (ownership map re-confirmed): demo@loramer.com owns a name-colliding "Influential Drones" copy vs the real one under cotebrandmarketing@; it is the DEMO fixture (not a real duplicate to merge). Existing note re-verified; leave as-is unless demo@ is cleaned up. src: 2026-07-11. open(known-fixture) [NP]
- CLIENT VALUE_MODEL (LORAMER_CLIENT_VALUE_MODEL_V1) — Layer 1 ✅ SHIPPED 2026-07-03 (ef6ab8f, migration 025): additive nullable jsonb value_model on client_context (online-purchase / offline-sales / lead, multi-select) + Lora ALWAYS-ON emit ("Client value model: …" in build-claude-context, never suppressed by business_descriptor) + -next multi-select & NON-DISMISSABLE HARD GATE (≥1 required before the client surface is usable). /api/context unchanged (generic spread stores it). DEPLOYED; Gate-B on-device PENDING (do NOT mark fully done). Layer 2 = QUEUED, not built: legacy /clients form field + server-side onboarding gate + the ROADMAP "Data Completeness Onboarding" meter / soft+hard-gate framework / per-source N/A / nudge engine (d5bf56e). src: value-model build 2026-07-03. Layer-1 done; Layer-2 open [LC]
- ✅ DONE 2026-07-03 (LORAMER_NEXT_WORKING_LAYOUT_V1, 9db6b0b) — per-client working-layout persistence in the -next CardEngine: debounced autosave + Done-flush + pending-flush on client-switch/tab-close + snapshot peel-off + Lesson-53 revalidate; mount loads the reserved 'Default' row (snapshots never hijack mount); no migration, no route change. Gate-B on-device PENDING. src: 2026-07-03. done [LC]
- ✅ DONE 2026-07-10 (LORAMER_NEXT_MOBILE_LAYOUT_V1, BUILT 2026-07-09; Gate-B verified 2026-07-10) — PER-BREAKPOINT (mobile sm) CARD-ORDER persistence in the -next CardEngine. ROOT CAUSE: CardGrid dropped onLayoutChange on mobile (`if (!isMobile)`) + regenerated the sm layout from cards[] every render → mobile drags never reached layout[], Done saved nothing. FIX (4 files, additive, NO migration): SavedView gains optional `layoutSm` (desktop stays `layout`); CardGrid captures a genuine mobile reorder into a SEPARATE slot via onLayoutSmChange (never desktop lg/md coords) + hydrates sm order from layoutSm else cards[]-order stacking (FIX-1 cumulative-height preserved); CardEngine layoutSm state → hydrate/debounced-autosave/Done-flush/buildWorkingView; /api/next/layouts stores layoutSm in the `view` JSONB. Desktop+mobile INDEPENDENT by design (Russ-confirmed); a layoutSm-less legacy row renders identically until reordered on mobile. queryBreakdown/metrics-query UNTOUCHED; -next preview-gated only (not live/reviewer). Gate-A GREEN: tsc+build exit 0; ordering loop proven (legacy=cards[] order · mount/breakpoint fire skips · drag→re-mount holds · convergence no-thrash · new-card appends · removed-card drops). Gate-B on-device VERIFIED 2026-07-10 (mobile persist loop HELD · desktop order untouched · legacy no-layoutSm row rendered intact) — MOBILE-PARITY GATE Gate-B CLOSED. src: DECISIONS PER-BREAKPOINT CARD ORDER + MOBILE-PARITY GATE, 2026-07-09; Gate-B verify 2026-07-10. done [LC]
- MOBILE-PARITY WATCH (guardrail) — any FUTURE -next page that persists card order via a NEW path OUTSIDE CardEngine reintroduces the bfe1a3d mobile-persist gap. Pages that REUSE CardEngine inherit the layoutSm fix BY CONSTRUCTION (safe). GUARD: new card-order persistence MUST go through CardEngine/layoutSm or explicitly carry its OWN per-breakpoint slot. NOT a current gap — Overview (dashboard-next/page.tsx, pageKey='overview') is the only CardEngine mount today. src: MOBILE-PARITY SWEEP 2026-07-10. standing [NP]
- ✅ DONE 2026-07-03 (LORAMER_RESUME_PROTOCOL_REPAIR_V1, 69856c3) — RESUME_INSTRUCTIONS canonical block freeze→blast-radius (the stale "live app FROZEN" line) + generator "8→10" comment; wrap re-stamp (CONTINUE_HERE/QUEUE/RESUME_INSTRUCTIONS + digest) → freshness gate TRUE 10/10. ⚠ Russ must RE-PASTE the updated block into Claude-app settings (RESUME_INSTRUCTIONS SWEEP CHECK). src: protocol-repair recon 2026-07-03. done [process]
- P2-A.1 ✅ DONE 2026-07-02 (LORAMER_NEXT_CHAT_POLISH_V1) — chat polish: (1) MARKDOWN rendering for Lora's replies via react-markdown + remark-gfm (bold/lists/tables; tables scroll horizontally inside the bubble on mobile via chat.module.css .md styles); user turns stay plaintext. (2) Ambient window now FOLLOWS the shared CardEngine date picker via a tiny client period-bus (src/lib/next/period-bus.ts): CardEngine setSharedPeriod() on picker change/mount → ChatLauncher getSharedPeriod() on open + 'loramer:period' subscribe → /api/chat body carries dateRange/customStart/customEnd (default LAST_30_DAYS until a picker is seen); Lora still fetches any explicit period via tools. -next only, ZERO backend. Gate-A: renderToStaticMarkup of the exact hour-table answer → real <table>/<strong>/<ul>, no raw ** ; period-bus channel test (getSharedPeriod + event) passed; build green. Gate-B (Russ on-device): the rendered table in the sheet + live picker→chat payload. src: P2-A follow-ups. done [NP]
- ✅ DONE 2026-07-02 (LORAMER_BREAKDOWN_INDEX_V1) — query_breakdown statement-timeout FIXED. ROOT CAUSE: the reader filters (client_id, platform, breakdown_type, date) but the only usable index was the 7-col conflict key (entity_level/entity_id before date/breakdown_type), so the planner seeked only the (client,platform) prefix and SCANNED the whole (client,platform) slice — huge for heavily-backfilled Google (geo/depth) → Postgres statement timeout (query_breakdown(hour,google) on Veterinary; Meta squeaked under on a smaller partition). FIX: migrations/024_breakdown_index.sql = CREATE INDEX CONCURRENTLY idx_metrics_daily_client_platform_bt_level_date (client_id, platform, breakdown_type, entity_level, date). APPLIED via a direct Postgres session (statement_timeout=0) — the MCP + SQL Editor both cap ~2min < the 269s build on the 27M-row/12GB table (the credential path took several tries; brackets around the DB password were the auth blocker). VERIFIED: indisvalid=true (215MB); EXPLAIN cost 140,637→5,222 (planner now seeks breakdown_type on the new index); Gate-B query_breakdown(hour,google,Veterinary,30d) 9-15s-timeout→83ms, Meta hour no-regression (1.2s). Affects ALL breakdown reads on heavy (client,platform) partitions — systemic fix, worsens-with-backfill averted. src: 2026-07-02 timeout diagnosis. done [LC]
- ✅ DONE 2026-07-02 (LORAMER_GOOGLE_HOUR0_NOTE_V1) — GOOGLE HOUR-0 CATCH-ALL honesty note. Google buckets non-hourly-segmentable campaign spend (Display, some PMax) into segments.hour=0 → hour "00" is inflated, not real midnight (Veterinary June: a Display campaign = $236.27 at hour 0 / $0.09 elsewhere; REAL — Σ24==anchor to the cent). FIX (additive-only, no data change): query_breakdown(google,hour) attaches a catch-all note + the tool description + breakdown-registry warn Lora. Gate-A: note present on (google,hour), (meta,hour)+others byte-identical; LIVE chat probe — deployed Lora now leads "hour 00 is NOT a real peak, it's a data artifact … do not bid-down on hour 00" instead of recommending a midnight bid-down. src: 2026-07-02 midnight-spike diagnosis. done [LC]
- ✅ DONE 2026-07-02 (LORAMER_BREAKDOWN_LEVEL_SCOPE_V1) — QUERY_BREAKDOWN ENTITY-LEVEL SCOPING (the double-count). FIX: additive queryBreakdown resolves ONE entity level (coarsest present via an index-covered probe; entityLevel override mirrors P1b video) + a stable value-asc tiebreaker on ranking ties. Gate-A: multi-level reconcile EXACT to coarsest-level DB Σ (Google hour $3,945.88→$2,427.36 campaign; Meta age $2,247.06 / hour $1,457.71 / device $2,247.10 == account); single-level search_term/keyword byte-identical, conversion_action data-identical (tied rows now deterministic), placement sub-cent float-noise, shopify geo now deterministic; google hour sub-second (217ms). Corrects the -next age card + Lora's multi-level numbers (see DECISIONS number-correction 2026-07-02). query_metrics/query_money unaffected (already entity-scoped). Original (superseded) note below:
  - [SUPERSEDED — fixed above] The additive queryBreakdown path SUMS all entity levels for a breakdown_type → double-counts multi-level breakdowns: Google hour all-levels $3,945.88 = campaign $2,427.36 + ad_group $1,518.52 (true = campaign level); ALSO inflates the Meta hour numbers Lora reports (summed across account+campaign+ad_set+ad — the "4 PM $437.68" figure is inflated) + pre-existing age/gender/geo(meta). FIX: scope additive breakdowns to ONE entity level (coarsest present, or an entityLevel arg like the P1b video projection); the new index already includes entity_level to serve it. Shared read-path (metrics-query.ts) — freeze-sensitive; STOP-and-confirm. src: 2026-07-02 timeout diagnosis (found while investigating). open [LC]
- -NEXT CONNECTION MGMT (LORAMER_NEXT_CONNECT_V1): F1 ✅ DONE 2026-07-12 (truthful state + working disconnect) · F2(a) ✅ DONE 2026-07-12 — Shopify(Branch A)+Woo Connect/Reconnect FROM -next (additive returnTo through shopify/auth+callback[Branch A ONLY]+woo/auth+woo/return; safeReturnTo open-redirect guard; Shopify Branch B install byte-identical; legacy byte-identical when returnTo absent; ClientPage enables Shopify/Woo, reuses account_id for reconnect incl. read_all_orders re-auth for Influential+Escential). F2b ✅ DONE 2026-07-12 (option B) — Meta+GA Connect/Reconnect FROM -next: ported the Meta account-picker + GA property-picker into -next ClientPage; additive returnTo through meta/auth+callback + ga/start+callback (safeReturnTo guard; legacy byte-identical when absent; ga cookie path=/ survives); HARDENED /api/clients/connections POST to delete-then-insert (fixes Meta reconnect 500 for -next AND legacy; first-connect byte-identical; touches only platform_connections); finalize REUSED (GA ga/connect unchanged). ⇒ ALL 5 platforms connect/reconnect/disconnect from -next. F3 ✅ DONE 2026-07-12 — GOOGLE-ADS DECOUPLER (new isolated /api/google-ads/connect/{start,callback}): native email/password users capture the owner adwords refresh_token post-login (no signIn('google'), no identity switch); callback mirrors auth.ts's google_tokens write exactly (indistinguishable downstream), refresh_token preserved on reconnect, open-redirect guarded, auth.ts login UNTOUCHED. -next Google row keys on the owner token (hasGoogleAdsToken). ⇒ CONNECT ARC F1+F2a+F2b+F3 COMPLETE. REMAINING: F3b [LC] = port the Google client→customer_id account picker (googleModal/connectGoogleAccount, legacy-only) into -next. ⛔ RUSS ACTION [EXT, gates the live round-trip]: add redirect URI https://app.loramer.com/api/google-ads/connect/callback to the GOOGLE_CLIENT_ID OAuth 2.0 Client in the Google Cloud Console (client-config, not consent-screen → should not trigger adwords re-verification; watch the still-open verification clock). src: master-audit blocker #1-3. open [LC]
- ⛔ FINISH-LINE QUEUE (2026-07-12 wrap, BLOCKS-first — the ORDERED front of queue lives in CONTINUE_HERE ═══ FINISH-LINE QUEUE ═══, authoritative): (1) F3b Google customer_id picker → -next · (2) Woo Shelley unstall · (3) Phase-3 completeness gate · (4) password reset (Resend) · (5) delete-client -next (soft) · (6) PRE-LAUNCH SECURITY HARDENING AUDIT · (7) cutover. Russ actions A–E also in CONTINUE. [LC]
- PRE-LAUNCH SECURITY HARDENING AUDIT [LC, slot RIGHT BEFORE cutover] — read-only whole-surface pass before the default-route flip: auth/session integrity (NextAuth JWT + native-auth allowlist gate on BOTH doors), secrets-never-leak (no key/token in any client response or error body — the /api/interest + connector pattern), TENANT/RBAC ISOLATION on EVERY data route (resolveAccess / #20 ownership-gate present + correct), input/injection, open-redirect/CSRF (safeReturnTo covers the connect returns; audit the rest), confirm every historically-flagged secret rotated (NEXTAUTH_SECRET, CRON_SECRET placeholder, reviewer token). Output = tagged findings; fix BLOCKS before cutover. src: master-audit T5/T6 + 2026-07-12 wrap. open [LC]
- Connect flow (F2/F3 above); saved-chats browser; logo upload; contacts; migrate user_notes→Facts. src: CONTINUE_HERE QUEUED. open [LC]
- Agency profile route (stub) → unlocks agency-level Knowledge UI. src: CONTINUE_HERE QUEUED. open [LC]
- Onboarding upload prompt / post-connect nudge layer (empty-states done). src: CONTINUE_HERE QUEUED, ROADMAP cross-cutting nudge. open [LC]
- Glossary batch (auto-save tooltip + Knowledge/word-budget note). src: CONTINUE_HERE QUEUED. open [LC]
- DECISION pending: user-delete = soft-delete+retention (current) vs immediate hard-purge. src: CONTINUE_HERE QUEUED. decision-pending [LC]
- Launch cleanup: remove/disable legacy /api/upload (broken pdf-parse v1) + /clients uploader when -next replaces legacy. src: CONTINUE_HERE QUEUED. open [LC]
- NICETY: soften encrypted-PDF rejection copy in /api/knowledge. src: CONTINUE_HERE QUEUED. open [NP]
- Privacy / no-training copy (folds into homepage unification). src: CONTINUE_HERE QUEUED. open [LC]
- Universal table SORT — every table sortable on every column (build into redesign tables, not legacy). src: AUDIT 2026-06-18, Flight-2#? . open [LC]

FLIGHT-2 FOLLOW-UP QUEUE (CONTINUE_HERE, from Russ 2026-06-18):
- #1/#6 Backfill AUTO-CONTINUE to completion (drop manual Resume; 20-lap cap = safety) + progress meter, ALL platforms. src: CONTINUE_HERE Flight-2. open [LC]
- #2 Lora content bleed on mobile+desktop (tables overflow) — responsive across ALL Lora surfaces. src: Flight-2. open [LC]
- #3 Mobile: no path to /clients cards grid (dropdown lacks "all clients") — mobile nav gap. src: Flight-2. open [LC]
- #4 Shopify month-view loads too slow — perf; check live-fetch-vs-metrics_daily/index first (pairs w/ Overview long-range timeout). src: Flight-2, AUDIT 06-18. open [LC]
- #5 Daily/weekly/monthly toggle on Shopify performance graph. src: Flight-2. open [LC]
- #7 Campaigns bottom-nav defaults to Meta; must default to BOTH when google+meta both connected, else the single one. src: Flight-2. open [LC]
- #8 "Clear" button under alert Lora surface — UNKNOWN behavior; read-only check (view-clear vs history wipe). src: Flight-2. open(investigate) [LC]
- #9 More Meta data — conversions (ATC/purchases/ROAS) not just graph+name on mobile campaigns>meta; benchmark TripleWhale/Shopify/Google. src: Flight-2. open [LC]
- #10 Re-auth button for ALL platforms (Google/Meta/GA reachability gap; cross-platform re-auth integrity audit, L54). src: Flight-2, OPEN-FOLLOWUPS. open [LC]
- Overview long-range load can time out ("Couldn't load" at Last 90d) — harden timeout/perf (combined multi-source fetch). src: AUDIT 06-18. open [LC]

═══════════════════════════════════════════════════════════════════
AUDIT_FINDINGS.md — master pre-launch punch-list (WS1→WS3 + held/post-meta/external/low)
═══════════════════════════════════════════════════════════════════
- WS1c STEP 2 — CATCH-UP LOOP — ✅ RESOLVED (repo-verified 2026-06-22): commit 513b980 (catchup route) + 577ffb4 (staggered crons) + 0d15bcf cron_runs sentinel; src/app/api/cron/catchup/route.ts + src/app/api/cron/status/route.ts + src/lib/cron-runs.ts all exist+wired. The AUDIT_FINDINGS WS1c#2 "PENDING" line is STALE → flipped to RESOLVED. src: AUDIT WS1c#2 (stale) vs commits. resolved [LC]
- #8 dimensional sync_state never stamps last_forward_sync_date (google_dimensional+shopify_dimensional NULL → no staleness signal). src: AUDIT#8. open [LC]
- #9 sync_state vs rows inconsistency (rows written but sync_state skipped). src: AUDIT#9. open [LC]
- WS2 #2 credential FAN-OUT on single (spurious) auth error — ✅ RESOLVED 2026-06-24 (LORAMER_CONNECTION_PROBE_BEFORE_FLIP_V1): live-probe-before-flip in recordConnectionAuthFailure for google+meta (probeMeta /me + probeGoogle listAccessibleAccounts, src/lib/connection-probe.ts); dead→flip, alive→heal-credential-wide, indeterminate→fail-safe; per-credential-per-invocation coalescing; classifier/scope-resolver unchanged. Gate A 6/6 + Gate B (prod cron healed 12/12 cote@ Meta conns, zero wrong flips). src: AUDIT#2. resolved [LC]
- WS2 #2b — connection-state probe PARITY audit — ✅ RESOLVED 2026-06-24 (read-only): Shopify + GA (incl. the GA direct cron/sync:616 path) PROVEN SAFE BY CONSTRUCTION (Shopify = getValidShopifyToken refresh-determination or a live Admin-API 401 = call-is-the-probe, per-shop_domain boundary; GA = invalid_grant from the real refresh POST only, per-client, can't false-flag a healthy token). Woo = the lone gap → #2c. src: WS2 #2 follow-up. resolved [LC]
- WS2 #2c — WooCommerce probe-before-flip — ✅ FIXED 2026-06-24 (LORAMER_CONNECTION_PROBE_WOO_V1): probeWoo (one gentle authenticated GET /orders?per_page=1, Basic auth = capture-path-identical, timeout, no loop) wired into the recordConnectionAuthFailure probe-gate for woocommerce — alive→no-flip+heal (per-client); dead(401)→flip; indeterminate→fail-safe; per-client coalescing; classifier unchanged. Gate A 4/4 vs live shelleykyle.com + Gate B (prod woo cron normal pass, 0 errors, no false-flip). ⇒ Lesson 60 now UNIVERSAL across all 5 platforms (meta/google/woo probe-gated; shopify/ga safe-by-construction). src: WS2 #2b. resolved [LC]
- #4 Meta breakdowns — PLACEMENT ✅ DONE (Slice1 c06d1c7 forward + Slice2 9cb038a history backfill, scaled to all reconcilable Meta clients); age/gender STILL live-only/held (Meta-adapter, until Meta decision). AUDIT#4 "not persisted" line updated to reflect placement done. src: AUDIT#4, inventory §6. partial [EXT]
- #6a Shopify test-order exclusion (confirm test:false syntax; fold into liveOrders + re-backfill). src: AUDIT#6a. open [NP]
- #6b /api/shopify/daily cancelled rule (pre-launch fast-follow; live chart must agree w/ Lora). src: AUDIT#6b. open [LC]
- #7 Woo deep tail 2016-10→2018-12 (Shelley) — DEFERRED (host PHP-fatal/500 on 2018-11 window) — adaptive sub-chunking under HARD GATE later. src: AUDIT#7, LAUNCH_PARKING. deferred [NP]
- #11 Dashboard rail "missing Google tab" single-ad-platform clients (~4-line fix, dashboard ~3784) — post-Meta (frozen). src: AUDIT#11, ROADMAP §Dashboard-rail. open(frozen) [EXT]
- ENV-TRUTH: Air .env.local blank/placeholder values (CRON_SECRET 11-char placeholder→now SET; 8 blanks: GA×3/Shopify×2/Stripe webhook+portal/reviewer token). src: AUDIT ENV. partial [LC]
- ENV-TRUTH: iMac value-level env audit + stamp HANDOFF block. src: AUDIT ENV. open [LC]
- #10 GA dimensional grains + GA/Meta entity-depth backfills (completeness depth). src: AUDIT#10. open [LC]
- #12 transient "Google data fetch failed temporarily" in Lora (06-12) — check Vercel logs (L15). src: AUDIT#12. open(low) [LC]
- #13 Shopify token read by user_email alone — ✅ CLOSED (repo-verified: getValidShopifyToken keys by user_email AND shop_domain, src/lib/shopify-token.ts:39-40/50-51/119-120, per #21). AUDIT#13 marked closed. src: AUDIT#13/#21. resolved [LC]
- #14 Shopify dead refresh token (2 clients efe036b4, bb9e2c31) — identify by name + reconnect. src: AUDIT#14. open [LC]
- #18 legacy live-data routes: request accountId not bound to owned client (caller-token-bounded; MCC breadth). src: AUDIT#18. open [LC]
- #19 query-metrics + CRON backfill wrappers: service-role + request id, no owner gate (CRON_SECRET-mitigated). src: AUDIT#19. open(by-design) [LC]
- #22 DEFENSE-IN-DEPTH: extract assertOwnsClient helper across ~9 routes + delete dead anon export + de-alias supabaseAdmin. src: AUDIT#22. open [LC]

═══════════════════════════════════════════════════════════════════
LAUNCH_PARKING.md
═══════════════════════════════════════════════════════════════════
- PRE-LAUNCH IDENTITY MATRIX: walk (a) MCC agency, (b) direct-grant single-account owner [FAILS empty picker; needs ListAccessibleCustomers path], (c) two-users-same-account — each with a real identity end-to-end. src: PARKING matrix, L44, ROADMAP pre-launch. open [LC]
- POST-META UI BATCH (frozen): Step1 backfill "complete" predicate investigation → Step2 completeness-semantics fix (complete=max-retrievable, show first-activity, Resume only when incomplete) → Step3 Woo Phase 2b UI trigger (BackfillControl on Woo row, behind HARD GATE). src: PARKING, AUDIT#4/#7. open(frozen) [EXT]
- PHASE-2b WIRING GATE: Woo into /api/backfill/run MUST carry force-no-store (L52) + route through breaker engine, never raw re-walk. src: PARKING. open [LC]
- PRE-LAUNCH managed-API hygiene audit: confirm Google/Meta/Shopify/GA back off cleanly on 429. src: PARKING. open [LC]
- PRE-LAUNCH-NICE: explicit Manage/chevron control on every client row (expand affordance). src: PARKING, ROADMAP Open-profile. open [NP]
- POST-LAUNCH: "Return Rate" tile mislabeled (returning-customer% vs refund-rate) — rename or compute refund-rate. src: PARKING. open [NP]
- POST-LAUNCH: left-nav IA rethink (function vs platform) before adding more platform tabs. src: PARKING. open [NP]
- /welcome "Let's go" hydration delay (loading skeleton / disable until hydrated). src: PARKING. open [NP]
- Meta audience-targeting data appearing in Claude responses — verify meta-intelligence end-to-end (reliable vs inferred). src: PARKING. open(investigate) [LC]
- Node url.parse() DEP0169 deprecation (transitive dep) — defer to dependency pass (Project 8). src: PARKING. open [NP]
- POST-FREEZE: Uploads managed malware scan + final security review (scan_status seam ready, no migration). src: PARKING, ROADMAP Project10. open(post-freeze) [EXT]

═══════════════════════════════════════════════════════════════════
ROADMAP PROJECTS (open items only; [x] in DONE appendix)
═══════════════════════════════════════════════════════════════════
P2 BILLING (STRIPE_BILLING_PLAN.md): enable Google Pay; Phase 5 gating (workspace cap/question counter/retention/flags + upgrade prompts) DEFERRED [NP]; Phase 6 go-live (activate, TEST→LIVE keys, LIVE webhook, RE-CREATE portal config LIVE + STRIPE_PORTAL_CONFIG_ID, smoke-test) [EXT/early-July]; capture sub.cancel_at into mirror IF surfacing "cancels on X" (L41). src: ROADMAP P2, AUDIT external, CONTINUE_HERE Stripe.
- ⭐ PROPOSED PRICING MODEL (2026-07) — docs/PRICING_MODEL_2026_07.md (soft-locked 2026-07-05, NOT enforced, PENDING Phase-5 gating). Ladder: Free $0/20Q/1-platform/30d/~5k-word-uploads · Starter $49·$470/250Q · Business $99·$950/750Q/+wyws · Pro $299·$2870/2500Q/+priority_support+bulk_export · Agency $199·$1900/500Q/10ws · Scale $999·$9500/2500Q/50ws · Enterprise custom. Two columns (business: Free/Starter/Business/Pro · agency: Agency/Scale/Enterprise); annual 20% off; FULL permanent history on ALL paid; every paid = all-platforms + value-models + provenance + ~20-25k-word uploads + Lora-on-Opus-4.8 (gated, see below); margins 80% realistic / 47% worst-case at Opus 4.8. Deltas from LOCKED: business $79→$99 & 100→750Q; Starter+Pro NEW; Free 5→20Q + uploads; history full on all paid (was 365d); flag remap wyws→business+, bulk_export+priority_support→Pro. Breaks no one (0 paid subs; all beta_unlimited). Enterprise trip line = ~$250k/mo managed spend OR an enterprise capability (SSO/SLA/custom-retention/security-review) — CONFIRMED (soft, 2026-07-05; revisit-able, not a hard commitment). Canonical ENFORCED matrix stays LORAMER_DECISIONS.md until Russ locks this. src: EOD 2026-07-05. proposed [LC]
- SPEND-LOGGER OPUS RATE (pricing prerequisite, LAUNCH-CRITICAL before any model switch) — src/lib/spend-logger.ts MODEL_PRICING has NO opus-4-8 entry (legacy claude-opus-4-6/4-7 $15/$75 only); computeCostUsd returns 0 for any UNMAPPED model → the moment Lora moves to Opus 4.8 (or Sonnet 5) those calls log cost_usd=0 = a SILENT spend undercount. ADD the correct opus-4-8 rate (~$5/M in · ~$25/M out per the proposal — VERIFY vs Anthropic published) BEFORE the switch. Pairs with P8 "Anthropic spend dashboard". src: EOD 2026-07-05 spend recon. open [LC]
- LORA MODEL A/B (banner stays Haiku) — the insight banner stays claude-haiku-4-5; moving Lora CHAT to Opus 4.8 is PENDING a Sonnet-vs-Opus A/B AND Phase-5 gating being LIVE (do NOT put paid self-serve tiers on Opus before caps are enforced). Real chat token shape (2026-07, n=160): input avg ~9.9k / p90 ~18.9k, output avg ~516 / p90 ~1000; prefix cache-hit on multi-turn (logged cost over-states real). src: EOD 2026-07-05. open [NP]
- MARKETING-COPY FIX (landing pricing page) — the Enterprise card still reads "Contract billing outside Shopify" (STALE: Stripe is the processor, Shopify never touches money). Outside the 3 pricing docs (marketing copy, not the matrix). Fix at the pricing-page pass. src: STRIPE §Open-items, EOD 2026-07-05. open [LC]
P3 INTELLIGENCE DEPTH: Google auction-insights(impression-share/overlap/outrank), bid-strategy; Meta placement[SHIPPED]/audience-targeting/bid-strategy/conversion-events; ad-creative details; device/geo/historical-trend in rowContext; WEB SEARCH for Claude (on-demand→auto-trigger); pre-built prompt library + WYWS digest; multi-KPI (JSONB array); [R1] GROWTH BLUEPRINT/"bomb" doc (subagent fan-out, needs real name); [R2] DAILY ALERT LAYER (audit-first, post-cron, cadence-adaptive). src: ROADMAP P3. open [LC except web-search/blueprint NP]
P4 EXECUTION LAYER: pause/enable/bid/negative keywords; pause/enable campaigns, budgets, bidding; "Claude Recommends" approve-checkbox→Execute. src: ROADMAP P4. open [NP]
P5 UX/POLISH: Meta picker sort+search; SMART tips/coachmark system + in-dashboard glossary (+Stripe Phase3 follow-ups a–e: empty-dashboard guidance, welcome-copy truth, path-chooser continuity, first-login progressive onboarding, per-row mapper selection); site-wide info tooltips; better loading skeletons; client-switch data refresh; user-defined Tier-2 dashboard cards; better empty states; metric-cards redesign; visual refresh; explicit open-profile affordance; state persistence (chart metric/granularity/lines/sort persist + restore-from-localStorage); chart-table column sync; demo mode; explainer page. src: ROADMAP P5. open [mostly LC]
P5 KNOWN BUGS: Combined-mode drill does nothing (drill by row's own platform); window-focus auto-refetch (disable Next focus revalidation). src: ROADMAP P5. open [LC]
P6 PLATFORM EXPANSION: Bing/TikTok/Amazon/LinkedIn/X/Pinterest/Snapchat/Reddit Ads (consider Unified API); WooCommerce[connector shipped]; Klaviyo; Stripe payments; GA Phase 6 disconnect route+button. src: ROADMAP P6. open [GA-disconnect LC; rest NP]
P7 AGENCY: client-list sort/filter/drag-drop order; cross-client insights; benchmark view; best-practice sharing; white-label; team access; custom domain; client portal; agent automations. src: ROADMAP P7. open [NP]
P8 TECH DEBT: advar→loramer localStorage rebrand (51 keys + migration); /admin user-management page; refresh-connection UX; Next.js 14.2.3 security upgrade; npm deprecations; error boundaries w/ friendly msgs; rate-limiting; Google Ads Standard Access (scale-time); Anthropic spend dashboard; webhook retry queue; revisit forceRefresh; unify 3 caches; /api/intelligence request dedup; tier-gate/batch extractProfileContext; Google date-path consolidation (resolveDateWindow everywhere); stale-intel-cache fix; dead platformData in /api/chat; Meta spend>0 filter hides ad-set structure; layout.tsx Georgia inline override; insight useEffect missing userNotes dep; landing copy "ads reimagined"; architectural /api/* audit pass; SYNC_STATE migration capture (010→011 gap). src: ROADMAP P8, AUDIT FINDs, CONTINUE_HERE. open [mixed]
P9 MEMORY & LEARNING: Phase3 dismissed-insights+pattern obs; Phase4 nightly learning loop+daily logs; memory-editor UX (collapsible/search/sort/pin/last-referenced/glossary-popover/merge/decay/sub-group/dedicated route); Phase2.2 changed-circumstances (3 Qs pending); Phase2.3 environmental-change detection (banned-metric signal); warm-start agency brain; "Website" field on Mer profile (ingest site w/ provenance). src: ROADMAP P9 + design docs. open [NP mostly; website field LC]
P10 UPLOADS: V1 ✅ SHIPPED via Uploads Pass B (repo-verified: /api/knowledge route + migration 017 uploaded_docs/upload_audit + separate-from-user_notes + magic-byte + 25MB + dedup + delete/soft-delete + audit + budgets + KnowledgePanel) + PDF extraction fix (3dde314, unpdf replaces pdf-parse — DOMMatrix bug FIXED). REMAINING: remove/disable legacy /api/upload (still broken pdf-parse v1) + /clients uploader when -next replaces legacy; V2 structured CSV/XLSX; V3 live feeds (Sheets/Notion/CRM); onboarding upload nudge. ROADMAP P10 V1 task list flipped to done. src: ROADMAP P10, UPLOAD_DESIGN, CONTINUE_HERE. partial(V1 done) [LC]
- MULTI-ACCOUNT per client (>1 account same platform): Phase 1 ✅ DONE (migration 005 added nullable account_id to metrics_daily; LORAMER_MULTIACCOUNT_PHASE2A_V1 writes it). Phase 2 = widen the metrics_daily conflict key to include account_id = SCOPING-ONLY (docs/scoping/multi-account-phase2.md, 2026-06-05, nothing executed). The rollup-only vs per-account-breakout vs both DECISION is NOT settled (scoping doc leans per-account but is explicitly "SCOPING ONLY"). src: ROADMAP:1369, docs/scoping/multi-account-phase1+2, migration 005. open-decision + open-build [LC]
P11 ATTENTION SURFACE v2: dismissible alerts (track in client_memory); severity tiers; consistent across panels. src: ROADMAP P11. open [NP]
P12 USER-DEFINED ALERT RULES: alert_rules+alert_events schema; rule builder UI; email digest; AI-suggested rules; agent automations. src: ROADMAP P12. open [NP]
P13 MOBILE QA CADENCE + pre-launch mobile checklist (auth/clients/dashboard/campaigns/keywords/shopify/floating-claude/profile/error-states + known-issues); mobile right-panel bottom-sheet sizing. src: ROADMAP P13. open [LC]
P14 CONVERSATION SURFACE: Phase1 remaining (drop legacy conversations JSONB after soak; extractProfileContext→new table); Phase2 cross-context references; Phase3 Ask-Claude continuity banner; Phase4 cross-surface attribution (per-message surface labels — docs/PROJECT_14_PHASE_4_DESIGN). src: ROADMAP P14. open [NP mostly]
P15 HUMAN SUPPORT: "Talk to a human" link+button (always visible) + response-time promise; App Store/pricing human-commitment copy; footer→support 1-click; agency/business/pricing marketing callouts; help-desk tooling at 50 customers. src: ROADMAP P15. open [LC]
P16 GLOBAL PREFERENCES: user_preferences table+UI; build-claude-context reads user-prefs-first; cross-client directive propagation; operator-model (Phase3/P9). src: ROADMAP P16. open [NP]
P18 CUSTOMIZABLE DASHBOARDS: dashboard_layouts table; Card abstraction; react-grid-layout (Shopify proving ground→all tabs); templates; cross-platform "My Overview"; AI-suggested layouts. src: ROADMAP P18, CONTINUE_HERE redesign(d). open [LC foundation; AI NP]
- ALWAYS-ON DRAG (extends P18) — persistent 9-dot drag handle on EVERY card, draggable ANYTIME with NO customize/edit-mode toggle to enter first (Russ's cited exemplar: "TW"). Applies across ALL dashboard tabs; extends the drag/reorder work in the -next CardEngine (Project 18). Deferred — NOT launch-blocking. src: 2026-07-10 remote-control bank. deferred [NP]
- UNIFIED CARD DESCRIPTORS (extends the CardEngine descriptor model) — the multi-source ROAS card (RoasBody) carries RICHER per-row basis descriptors than the generic stat subtitle (a SINGLE line via CardConfig.subtitle / styles.roasBasis). DIRECTION: promote the richer descriptor capability to a SHARED pattern so ALL cards can carry multi-line / structured descriptors, not just ROAS. TRIGGER to build: when a SECOND card needs descriptors beyond one subtitle line. Deferred — NOT launch-blocking. src: 2026-07-10 (LORAMER_NEXT_MER_SUBTITLE_V1 follow-on). deferred [NP]
P19 CANVA CLOSED-LOOP / design phase (2027+ horizon). src: ROADMAP P19, Redesign post-launch. open [NP]
P20 MULTI-USER WORKSPACES: workspaces+workspace_members; backfill user_email→workspace_id; Owner/Member roles; invite flow; audit log; Viewer/Billing roles; SSO (Enterprise). (overlaps RBAC LC item.) src: ROADMAP P20. open [NP/LC overlap]
P21 EXPORT & SHARING: Markdown/txt → PDF/Word/XLSX → HTML-email/bulk → scheduled-digest/JSON/white-label. src: ROADMAP P21. open [NP]
P22 PROMPT CACHING: Phase3 two-tier (1hr TTL profile/memory) + conversation-prefix breakpoint — deferred until cache-hit data warrants. src: ROADMAP P22. deferred [NP]
DATA COMPLETENESS ONBOARDING (customer-facing surface): non-blocking progress meter (green/backfilling%/red/N-A); N/A path; notification state machine (stop only on connected|N/A); human-assist CTA. src: ROADMAP §Data-Completeness-Onboarding. open [LC]

═══════════════════════════════════════════════════════════════════
DATE-GATED (CONTINUE_HERE ⏰ — do not slip)
═══════════════════════════════════════════════════════════════════
- [DG] META TOKEN — ✅ REFRESHED 2026-07-01 via /api/admin/meta-token-write (updated_at 07-01, new cliff ~2026-08-30; 12 ad accounts ride it — the earlier "7" was stale). Verified stored + live-valid (fb_user_id 10242550452717848). Re-armed: next deliberate refresh ~2026-08-25 via the admin valve. NOTE the app's self-serve reconnect is REPAIRED 2026-07-02 (LORAMER_META_CALLBACK_ONCONFLICT_V1 — callback onConflict fix shipped; Meta review approved, freeze lifted for that file). src: CONTINUE_HERE date-gated#1, 2026-07-01. re-armed
- [DG] cote@ META TOKEN early-refresh — verify ~2026-07-10 (early-refresh due); hard expiry ~2026-08-30 (to-confirm). NON-BLOCKING. Cross-ref the META TOKEN [DG] line ABOVE (refreshed 2026-07-01, cliff ~08-30, next deliberate re-arm ~08-25 via the admin valve) — this is the early verify of the timeline, NOT a new cliff. src: 2026-07-09 remote-control bank. open(verify)
- [DG] GOOGLE ADS Tool Change Form submitted 2026-06-19 (permissible-use external/reporting); MONITOR russ@; if no reply by 2026-06-26 resubmit/escalate; GATE: no external self-serve Google until API Center reads external. src: CONTINUE_HERE#3. open
- Influential Drones Meta probe — SETTLED alive-but-empty (optional unfiltered confirm). src: CONTINUE_HERE#2. open(optional)
- GA token VERIFY after 2026-06-20 (4 re-authed tokens still refreshing). src: CONTINUE_HERE STANDING. open
- Post-Meta-approval queue: flip NEXT_PUBLIC_SHOW_CONNECTION_HEALTH_UI=1 + redeploy; Meta app Dev→Live ✅ DONE 2026-07-02 (app published; fresh-external verified — Renee non-role connect, ads_read granted); publish gov-policy page; homepage unification; dashboard visual reconciliation; URL-state nav workstream; read_all_orders+read_inventory scope batch + Shopify resubmit + targeted re-auth → deep re-backfill + sell-through. src: CONTINUE_HERE STANDING/post-approval. open [EXT]

═══════════════════════════════════════════════════════════════════
OPEN FOLLOW-UPS / CARRY-OVER (CONTINUE_HERE)
═══════════════════════════════════════════════════════════════════
- Woo revenue reconciliation (TOP-OF-QUEUE) — repo-verified 2026-06-22: (a) order-count-vs-items labeling FIXED (4ba1bf9 LORAMER_WOO_ORDERS_VS_ITEMS_LABEL_V1 + 218d091; totalOrders=saleOrders.length distinct); (b) product-net CLOSED by Fix-1b (3e74e0b, Σproduct≡account o.total basis). REMAINING = the WC-Analytics-API exact-subtotal-net-to-match-her-admin path (no wc-analytics code exists in repo) = the BANKED Path-1 subtotal re-base (near-zero likelihood). src: CONTINUE_HERE TOP-OF-QUEUE(1) + commits. mostly-closed; WC-Analytics path banked [LC]
- Budget Utilization — Meta card "$500/day" for $500 LIFETIME campaigns (~30×); daily vs lifetime mis-read; diagnose now, fix POST-Meta (frozen). src: CONTINUE_HERE(2), Flight-2. open(frozen) [EXT]
- Error-path false-zero hardening — captured-read failure returns {connected:false} could show $0 not "couldn't load"; MUST-HARDEN-BEFORE-LAUNCH. src: CONTINUE_HERE(3). open [LC]
- token-dedup hardening (Escential cohort/reviewer duplicate rows). src: CONTINUE_HERE. open [LC]
- Escential SPEND-VIEW double-count (two client rows) — the account exists as TWO clients rows: "The Escential Group" (c39ee088, google/meta/shopify/ga) + "Escential Group" (bb9e2c31, shopify-only dup). Per-client rollups (e.g. anthropic_spend_log: $4.10 vs $0.0027) SPLIT Escential across both → merge/alias for a clean per-account view. Related to token-dedup above + the 18-connection hygiene audit. src: EOD 2026-07-05 spend recon. open [LC]
- cross-platform re-auth integrity audit (Google/Meta — L54). src: CONTINUE_HERE, Flight-2#10. open [LC]
- machine-parity pass (Air & iMac node/CLI — L56). src: CONTINUE_HERE. open [LC]
  - iMac .env.local CRON_SECRET still holds the SUPERSEDED value 983e…c2f1 (yesterday's). Prod is now 71ec…cbbe (rotated + behaviorally verified 200/401 on 2026-06-27 from the Air). The iMac will 401 on any local cron/probe/Gate-A work until its .env.local CRON_SECRET is updated to match prod. FIX next iMac session: copy the current value in, then run the behavioral 200 probe to confirm (docs/LORAMER_SECRET_ROTATION_RUNBOOK.md). open [LC]
- Shopify privacy disclosure (privacy/page.tsx) must list read_all_orders before broad launch. src: CONTINUE_HERE. open [LC]
- "Lora explains why same-thing numbers legitimately differ" — future; precondition numbers reconcile. src: CONTINUE_HERE(8). open [NP]
- Intraday "today" freshness top-up for Woo (bounded current-day). src: CONTINUE_HERE(9). open [NP]
- activePlatform sibling-axis read-only check (carried-valid tab render empty on ad→ad switch). src: CONTINUE_HERE(11). open(investigate) [LC]
- AUDIENCE VISION — PARKED, compliance-gated (DPAs/GDPR/CCPA/CAN-SPAM/Shopify-PCD/Meta-Google audience terms; lawyer-first; NOT now). src: CONTINUE_HERE(7). parked [NP]
- query_breakdown 1.2 modes (per-value trend, multi-window PoP, negative-keyword/waste). src: CONTINUE_HERE, QUERY_METRICS_REFERENCE "Later". open [NP]
- Wire Claude fully onto query layer; retire window-fetch + 15-min cache in /api/intelligence (Historical Engine Phase3). src: HISTORICAL_DATA_ENGINE_DESIGN §6, HANDOFF. open [NP]
- New-platform adapter template (Triple Whale/LinkedIn/TikTok inherit history+comparison) — Historical Engine Phase4. src: HISTORICAL_DATA_ENGINE_DESIGN. open [NP]
- LORAMER_COMPLETENESS_AUDIT_PLAN_V1 gap-map (per real client×source PRESENT?/RIGHT? + BLEEDING/RECOVERABLE/BOUNDED-LOST classification) — the read-only audit that feeds this queue. src: LORAMER_COMPLETENESS_AUDIT_PLAN_V1. open [LC]

═══════════════════════════════════════════════════════════════════
DESIGN-DOC OPEN BACKLOG (the ~8 unbuilt items beyond what's above)
═══════════════════════════════════════════════════════════════════
- Shopify LTV Phase 2 (true LTV, abandoned-cart funnel value, cohort retention) — needs new fetch. src: SHOPIFY_LTV_DESIGN. open [NP]
- GA connector Phase 6 disconnect (/api/ga/disconnect + button). src: GA_CONNECTOR_DESIGN, HANDOFF open. open [LC]
- ConnectionPill extract (from connector architecture audit). src: CONNECTOR_ARCHITECTURE_AUDIT. open [NP]
- PROJECT_9_PHASE_2_2 — 3 open questions pending Russ before build. src: PROJECT_9_PHASE_2_2_DESIGN. decision-pending [NP]
- Launch consolidation Phases 4-5 (login evolution: email/password secondary, OAuth-as-discovery) — wait for plan after GA. src: LAUNCH_CONSOLIDATION_DESIGN. open [NP]
- FLIP program staged cutover post-Meta+Google (current app = launch vehicle). src: LORAMER_NEXT_FLIP_PROGRAM. open [EXT]
- NAV regroup invariant — per-connection nav visibility (every connected platform gets a tab; combination logic may only ADD aggregate views) — homes hasBoth violation + Shelley C/D. src: LORAMER_NAV_REGROUP_PLAN. open(frozen) [EXT]

═══════════════════════════════════════════════════════════════════
LIVE + BREADTH WORKSTREAM (active design of record 2026-06-26)
═══════════════════════════════════════════════════════════════════
- ACTIVE DESIGN OF RECORD: docs/LORAMER_LIVE_BREADTH_UNIFIED_DESIGN.md (LORAMER_LIVE_BREADTH_UNIFIED_DESIGN_V1, LOCKED 2026-06-26). Direction B locked. Build per its §11 phase order: (1) consolidation pre-work → (2) breadth → (3) live spine → (4) live UI(-next) → (5) intelligence reshape (freeze-gated). Read it before proposing any Live/Breadth work. src: design doc. open(active) [LC]
- CONSOLIDATION PRE-WORK (Phase 1) — ✅ COMPLETE 2026-06-26, freeze-safe, zero-change proven: (1a) shared reconcileDay(grainMetric,anchorMetric,{abs,pct,posture})→{within,delta,action} (reconcile-day.ts, LORAMER_RECONCILE_DAY_V1) wired into the 5 AD writers (google-campaign BLOCK; google-adgroup-ad/meta-campaign/meta-adset-ad/meta-placement FLAG); shopify-dimensional HALT left as-is. (1b) shared fetch/paging/retry: gaql-with-retry.ts gaqlWithRetry (2 google writers) + meta-graph-paged.ts metaFetchAllPaged (3 meta writers, guard param preserves adset-ad's 200 cap, single-source RETRYABLE). Both proven OLD-vs-NEW (finalized + flag windows, stats+flagged[] byte-identical). src: design §5/§8/§11. ✅ done [LC]
- FUTURE (banked from Phase 1b, NOT done — both behavior changes, separately gated): (1) UNIFY THE TWO GOOGLE RETRIES — backfill gaqlWithRetry (regex RESOURCE_EXHAUSTED|429|rate, tries=4, 1000*2^i, no log) vs forward src/lib/google-retry.ts withGaqlRetry (gRPC {4,13,14}+richer regex, attempts=3, linear 400*(i+1), L15 logging); pick ONE policy (withGaqlRetry's code-based + logging is arguably better) → behavior change, verify before adopting. (2) META 100/subcode-1487534 NARROW-AND-RETRY — all meta writers currently THROW on it (parity); a "reduce the window + retry" fix lives at the CALLER's monthChunks loop (window narrowing), not the fetch primitive → separate behavior change. src: design §11, this session. open [NP]
- LIVE SPINE (Phase 3): sibling live store keyed by as_of + live reconcile primitive (label-as-of/baseline-band, NEVER equality) + query_live tool + truth-spine (self-labeling tool results + hard system-prompt rule + structural post-hoc provenance check); ephemeral snapshots, captured-wins-on-overlap. src: design §1-6. open [NP]
- STANDARD ACCESS — PROMOTED LAUNCH-CRITICAL (was scale-time) for the Live workstream: gated/scoped live pulls stay within Basic (15k ops/day); a PERSISTENT live env needs Standard Access OR a hard per-client polling cap. Blocked on the Google Ads Tool Change Form; Standard-Access RMF answer pack + Project-21 attach here. src: design §9, GOOGLE_ADS_TOOL_CHANGE_FORM_ANSWERS. open [EXT]
- GOOGLE ADS STANDARD-ACCESS TRACK (INVESTIGATE — not decided): (a) API Center shows NO permissible-use field (it's a TOKEN attribute, not a UI field — see DECISIONS "GOOGLE ADS API CENTER"); (b) the Tool Change Form is a support-queue EMAIL case — status lives ONLY in the API-contact inbox (support@/hello@loramer.com), no API Center status screen; (c) INVESTIGATE (not decided): the current path MAY be Access-level dropdown → Apply for Standard DIRECTLY (external-use declared in that form) rather than waiting on the tool-change verdict — VERIFY against real account state (current dev-token access level + any API-contact email reply via Gmail) BEFORE treating the prior "Standard blocked behind tool change" decision as superseded; (d) FLAGS: RMF applies to Standard ONLY (possible non-compliance fees; read-only likely not "full-service" so creation/mgmt RMF may not apply — confirm with Compliance if unsure); the API-contact inbox IS the compliance channel — unanswered requests can downgrade/terminate the token. src: 2026-07-09 remote-control bank. open(investigate) [EXT]
- GA FOUNDATIONAL: GA persists ONLY account totals today (all GA dims live-only, AUDIT page-level gap) — GA breadth needs the first-class-session/user-columns-vs-extra DECISION at build. src: design §7, AUDIT:126. open [LC]
- DEAD `raw` jsonb column on metrics_daily — nothing writes it; repurpose-for-raw-payload-provenance OR drop. src: design §8 Appendix. open [NP]
- entity_level CHECK/enum — it is free text today (a typo would silently fork a grain); add a constraint with breadth. src: design §7. open [NP]
- BREAKDOWN REGISTRY — documented per-breakdown_type {attach entity_level, value encoding, source field}; conventions vary today (Google terms→ad_group, Meta placement→campaign, Shopify geo→account; encoding raw/composite/ISO). src: design §7. open [LC]
- iMac⇄Air machine parity + PROD-GOOGLE-SECRETS fork (banked this session): the iMac local env can't run the live Google path (its OAuth app ≠ prod's that minted the refresh tokens → unauthorized_client) and had a placeholder→legacy-disabled service key (fixed to sb_secret_). Gate A for Google/Meta writers must run as a PROD dry-run (zero writes) OR Russ provides prod Google client_id/secret locally. Decide the standing approach + reconcile Air parity (L56). src: this session. open [LC]
- 18-CONNECTION HYGIENE AUDIT (banked): a per-(client,platform,account) sweep of all connections — dup/demo twins (e.g. Influential real 5bb9b2ff vs demo 2617b163), stale tokens, account_id integrity — feeding a clean canonical connection list. src: this session, Wave0 dup findings. open [LC]
- DEMO-TWIN 2617b163 (Influential Drones demo) — its Google campaign grain is shallower (camp_min 2026-06-10) than its ad_group/ad data (back to 2026-03) → ad_group/ad drain flags anchor_missing on pre-06-10 days (captured FLAG-NOT-BLOCK, not lost). Campaign re-drain for this one demo client to deepen the anchor (or confirm it's a demo we don't backfill). src: this session google_adgroup_ad tick. open(low) [LC]
- INFLUENTIAL DRONES connection health — real 5bb9b2ff meta account stops 2025-09-02 (token/connection edge); campaign/adset drains lap fine for its active range (no HALT) but verify the connection is healthy vs needs reconnect, per Lesson 60 (live probe before any "broken" claim). src: this session meta ticks. open(verify) [LC]
- STANDING RULE — READ-FIRST EXISTENCE CHECK: before building any new writer/route/adapter, grep the repo for an existing one first (this session the Meta campaign writer ALREADY existed unwired; building fresh would have duplicated it). Promote to a Lesson at next wrap. src: this session. standing [process]
- STANDING RULE — VERIFY-EXTERNAL-UI-BEFORE-INSTRUCTING: before telling Russ to do something in an external dashboard (Vercel/Supabase/Google/Meta), verify the actual current UI/state first; do not instruct from an assumed UI (this session: assumed the legacy JWT was the service key; the project had legacy keys disabled → needed the sb_secret_ key). Promote to a Lesson at next wrap. src: this session. standing [process]

## Parked Ideas (not committed — product bets, not scoped, not decided)
- PARKED IDEA (not committed — Russ's thought 2026-06-27, explicitly "not sold on it myself, don't want to forget it"): "System of Record" slim product — a stripped-down sibling to LoraMer: full historical backfill + forward capture + permanent store, but NO Lora/Mer analysis layer. Pure data custody, no intelligence surface.
  - Adjacency: this is the existing bedrock brand principle (LoraMer as permanent system of record; platforms purge ~37mo, we keep from connect-day forever) sold AS ITS OWN PRODUCT — custody without analysis.
  - Architecture note: the capture/drain/store layer being built in the Data Completeness Program IS this product; it would be LoraMer with the intelligence surface removed, not a new build.
  - Possible angles (unevaluated, for future context): cheaper lead-in tier to full LoraMer; agency white-label data-warehouse; compliance/recordkeeping angle for businesses needing legally-retained ad-spend history.
  - Status: PARKED. Not scoped, not decided. Revisit when considering tiering/pricing or productizing the permanent-store principle. src: this session.

═══════════════════════════════════════════════════════════════════
DONE — DO NOT REBUILD (appendix; representative — full list in ROADMAP Completed Archive + AUDIT RESOLVED)
═══════════════════════════════════════════════════════════════════
Shopify App Store approved+live; Google Ads/Meta/Shopify/GA-V1 connectors; Universal Intelligence Layer + /api/intelligence + 15-min cache + insight banner + sidebar/floating Claude; HARD CONSTRAINTS block; cross-panel memory; Project3 Steps 2a–2g + 3a–3d (search-terms, conv-attr, audience, demographics, RSA, PMax groups+combinations, geo, device, hour, impression-share); Shopify net-sales + deeper-signals + abandoned-checkouts + cancelled-accuracy(#6) + product-refund-net(Flight1) + deep-backfill (3 stores $0.00 residual) + customer-mix-fix + read_all_orders scope; canonical date-range; prompt caching (Project22 Phase1+2); Project14 Phase1 conversation unification + soft-delete; Project9 Phase2 memory; query_metrics + query_breakdown tools + ownership gate; Historical Engine Phase 0a (5 fwd adapters) + 0b (Google/Meta/GA backfill + in-app BackfillControl); cron per-platform split (WS1c step1) + catchup loop (step2) + cron_runs sentinel (WS1b-1) + §A normalize guard; Woo forward accuracy (status/net) + backfill engine + atomic breaker (migrations 012/013/014) + captured-read E1/E1.1; connection-health engine (dark); GAQL retry; GA re-auth; Stripe Phases 0–4 (entitlements/webhook/checkout/portal, TEST); off-site R2 backup + Supabase Pro backups; CRON_SECRET rotated; Meta compliance Phase1+2 (deauth/data-deletion); Meta scope read-only + App-Review submitted; Google OAuth adwords approved; demo@ provisioned; NAICS Pass3; Uploads Pass B (/api/knowledge) + PDF unpdf fix; RBAC foundation slice1A (migration 018); -next Increment-1 (portfolio + per-client Overview + Channels grid 1B–1K); ownership gates #16/#20; Woo all-products (Fix-1a) + product refund-net (Fix-1b); Meta placement persist Slice1 (forward) + Slice2 (history backfill, scaled to all reconcilable Meta clients). Lessons 1–59 (see LORAMER_DECISIONS.DRAFT.md). Project17 popover RESOLVED.
- Account-grain FORWARD-ONLY Meta backfill — Inside / Glenn Stearns / Ogmentor — DONE 2026-06-23 (LORAMER_DATA_COMPLETENESS_META_BACKFILL_INSIDE_GLENN_OGMENTOR_V1): account grain to floor (Inside 403d→2023-10-17 $9,158.26 / Glenn 113d→2024-09-23 $5,179.11 / Ogmentor 202d→2025-06-16 $3,617.55) + placement grain floor→yesterday (12,651 campaign×placement×day rows, conversions=0, reconciled FLAG-NOT-BLOCK, 1 flagged day Δ$0.03). Thought Streams = truly new (nothing owed); Influential Drones = RESOLVED 2026-06-24 (see next line).
- Influential Drones Meta — RESOLVED 2026-06-24 (LORAMER_CONNECTION_STATE_PROBE_SAFEGUARD_V1): connection ALIVE (capture-path probe HTTP 200 over 2024-02-01..2025-07-24, zero oauth_190); warehouse reconciles to the live API TO THE PENNY ($8,412.89, 2024-09-17→2025-07-24); pre-2024-09-17 = true zero, interior gaps = zero-spend days. Stored health='reconnect'/oauth_190 was STALE (WS2 #2 transient-190 fan-out) — cleared to 'healthy'. NOTHING owed; do not re-raise as blocked. (Generator WS2 #2 still queued, separate.)
- GOOGLE CONV-ACTION CATEGORY NAME — native forward emission RESOLVED 2026-07-08 (LORAMER_GOOGLE_CONV_ACTION_CATEGORY_NAME_V1, fae78f8): the forward writer (google-conversion-action.ts) emits extra.conversion_action_category_name NATIVELY (decoded from the raw ConversionActionCategory ordinal via google-ads-api enums; raw ordinal retained byte-for-byte). DB-VERIFIED 2026-07-08 — 62/62 fresh conv-action rows synced post-deploy (synced_at > 2026-07-05 20:30 UTC) carry the name, named_fresh==total_fresh across sync-days 07-06 (14/14) / 07-07 (31/31) / 07-08 (17/17), raw ordinal retained 62/62; the upsert replaces `extra` wholesale so a fresh named row proves the WRITER, not the 120-row backfill. Was the ⭐ TOP VERIFY at the ACTIVE-WORKSTREAM head; moved here on resolution. The Escential google-forward stall (the separate thread bundled in that same item) was diagnosed + RESOLVED 2026-07-08 — not a stall (see next entry). src: verify 2026-07-08.
- ESCENTIAL GROUP (c39ee088) GOOGLE-FORWARD "STALL" — RESOLVED 2026-07-08: NOT a stall; the 2026-07-05 flight-note premise is FALSIFIED. Google forward capture is HEALTHY + CURRENT: sync_state google last_forward_sync_date=2026-07-07 (parity with the Bath Fitter baseline), backfill_complete to the 2015-06-05 floor, no block, no lease held; platform_connections google health='healthy', last_ok_at 2026-07-08 08:18, last_error_at/last_error_code=null; warehouse latest google date 2026-07-07 synced 2026-07-08 08:18. The "0 conv-action rows" is a TRUE-ZERO, not a defect: the conv-action PERSIST writer (T0.1, LORAMER_GOOGLE_CONV_ACTION_IS_PERSIST_V1, live 2026-06-29, forward-only, filters conversions>0) has had nothing to write because Escential's Google conversions dropped to 0 after 2026-06-26 (9 conversions total in the prior 30d, none since) — the writer went live 3 days AFTER conversions already stopped → 0 conv-action rows EVER, correctly (vs Bath Fitter 98 rows / 639 conversions). The "06-26" in the flight note = Escential's LAST-CONVERSION date, misread as a capture-stall date. All diagnosis reads read-only 2026-07-08. PRODUCT-SURFACED NOTE (not a bug): Escential's Google conversions going to ~0 after 2026-06-26 is a CLIENT-ACCOUNT fact — most likely conversion tracking was paused/removed in their Google Ads account; Lora should surface this to the client (prompt-honesty), never treat it as missing capture. src: diagnosis 2026-07-08.
- MOBILE GATE-B (LORAMER_NEXT_MOBILE_LAYOUT_V1, bfe1a3d) — ✅ DONE, on-device Gate-B verify PASSED 2026-07-10: mobile drag→reload HELD the sm order; a mobile reorder left the desktop (lg/md) order UNMOVED; a legacy no-layoutSm row rendered intact (cards[]-order stacking). The MOBILE-PARITY GATE Gate-B for the per-breakpoint card-order build is CLOSED (the build item LORAMER_NEXT_MOBILE_LAYOUT_V1 in the -next active region is flipped to ✅ DONE to match). src: on-device verify 2026-07-10.
- MOBILE-PARITY SWEEP — ✅ DONE 2026-07-10 (read-only): NO OTHER GAP — exactly ONE card-order persistence path in -next (react-grid-layout in CardGrid → mounted only via CardEngine on Overview → written only by /api/next/layouts); the class "persists card order" has a single member, already fixed by bfe1a3d (LORAMER_NEXT_MOBILE_LAYOUT_V1). Fulfils the MOBILE-PARITY SWEEP queue item; the standing guardrail is the MOBILE-PARITY WATCH item in the -next active region. src: read-only sweep 2026-07-10.
- MER TILE — d-roas → MER headline + basis subtitle (LORAMER_NEXT_MER_SUBTITLE_V1, d4a5a17) — ✅ DONE 2026-07-10, Gate-B PASSED on-device: the default Overview 'd-roas' tile renders headline 'MER' + a two-line basis 'Marketing Efficiency Ratio · blended revenue ÷ all ad spend' with NO clip on mobile (sm); value 3.13x (= 2441 ÷ 779) unchanged; delta-priority intact (delta wins when comparing); other stat tiles unchanged. RESOLVES the d-roas/'roas' naming collision (distinct from the 'ROAS — multi-source' card). Mechanism = additive CardConfig.subtitle + StatBody styles.roasBasis render (B2 delta-priority); STAT_METRICS untouched, -next only. src: Gate-B on-device 2026-07-10.
- WOO SHELLEY RECENT-DAYS CAPTURE "STALL" (the ~8 missing-days / ~3.5% revenue-gap symptom) — ✅ RESOLVED–NOT-A-BUG 2026-07-10, confirmed by LIVE SOURCE (not derived). The recent-120d picture: 16 MISSING-ROW days (2026-03-12→05-13) + 19 ZERO-REV days (05-15→07-09), 0 null; forward capture CURRENT (sync_state woocommerce last_forward_sync 2026-07-09), connection healthy. A read-only GET /wp-json/wc/v3/orders probe of shelleykyle.com over ALL 16 missing-row dates (Basic auth capture-path-identical, sale-set {completed,processing,refunded}, NO writes) returned 0 sale-status orders on EVERY date (2026-03-21 had 1 CANCELLED order, correctly excluded) → TRUE no-sale days under the false-zero discipline (woocommerce-backfill.ts:227 "no sale that day → write nothing"). ZERO-REV days = refund-net-zero / $0-net days. NO capture gap, NO re-backfill needed. SYMPTOM RECORD CORRECTED: it was 16 missing-row + 19 zero-rev days (all explained), not "~8"; the "~8 / 3.5%" figure was imprecise/stale and is NOT a capture gap. Any residual ~3.5% is the KNOWN Woo revenue-BASIS delta (Woo net INCL shipping/tax vs her admin; the WC-Analytics/markdown path), tracked separately — NOT missing capture. SEPARATE + STILL OPEN (NOT closed by this): the pre-2018 DEEP TAIL — woocommerce_backfill stalled 2018-12-13→~2015 (onboard-divergence; the "WOO SHELLEY … RE-CAPTURE STALL" open [LC] item) + woocommerce_money/woocommerce_variant BLOCKED on her host's 500 at 2018-12-10 (the "#7 Woo deep tail" deferred [NP] item). src: live read-only probe 2026-07-10.
